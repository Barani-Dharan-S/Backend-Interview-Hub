const lab=(id,level,title,situation,goal,steps,commands,delivery,followups,traps)=>({id,level,title,situation,goal,steps,commands,delivery,followups,traps});
export const cicdLabs=[
lab('cicd-lab-001','L1','Build a CI pipeline for Spring Boot','A Spring Boot repository has no automated validation.','Create a pipeline that checks out code, builds and runs tests.',['Checkout','Restore Maven dependencies','Run unit tests','Package JAR','Publish test/build evidence'],['./mvnw test','./mvnw package'],'I would start with fast deterministic validation before adding deployment.',['What should happen on test failure?','Why keep test reports?'],['Do not deploy when the required build stage failed.']),
lab('cicd-lab-002','L1','Build a React CI pipeline','Frontend changes are merged without consistent lint/build checks.','Automate install, lint, test and production build.',['Checkout','npm ci','lint/test','npm run build','Store build artifact'],['npm ci','npm run build'],'The frontend pipeline should reproduce the same clean install and production build every time.',['npm install vs npm ci?','Where does the dist artifact go?'],['Do not use an uncommitted dependency lockfile.']),
lab('cicd-lab-003','L2','Build and scan a Docker image','The application passes tests but its container has never been scanned.','Build an immutable image, scan it and publish only if policy passes.',['Run application tests','Build image tagged with commit SHA','Scan image','Push to registry','Record digest'],['docker build -t registry/app:$GIT_SHA .','docker push registry/app:$GIT_SHA'],'I want the exact image tested and published to be identifiable by immutable metadata.',['Why use commit SHA?','What should block publication?'],['Do not deploy latest as the only version identifier.']),
lab('cicd-lab-004','L2','Promote the same image from staging to production','Staging and production currently rebuild the application independently.','Change the process so production uses the exact staging-validated image.',['Build once','Deploy staging','Run smoke tests','Capture image digest','Promote digest to production'],['kubectl set image deployment/api api=registry/api:$DIGEST'],'Promotion should change the environment target, not the application bytes.',['How do you prove the same image ran?','How do you rollback?'],['Never assume two tags necessarily refer to the same immutable content.']),
lab('cicd-lab-005','L2','Add a production approval gate','The organization requires a human approval before production.','Protect the production environment while keeping preceding stages automated.',['Complete CI','Deploy staging','Run quality/smoke gates','Require protected approval','Deploy production','Verify'],['# conceptual\nproduction requires approved promotion'],'Approval belongs at the risk boundary, not after every tiny pipeline step.',['What evidence should the approver see?','Can the approval be bypassed?'],['Manual SSH deployment outside the pipeline undermines governance.']),
lab('cicd-lab-006','L2','Debug a failing CI build','A build passes locally but fails in CI.','Find the first environmental or dependency difference.',['Compare Java/Node versions','Inspect lockfiles','Read first meaningful error','Reproduce in runner-like environment','Fix and rerun'],['java -version','node --version','npm ci'],'I compare the execution environment before changing application code.',['Why can local success mislead?','What should be pinned?'],['Do not randomly add retries to deterministic build failures.']),
lab('cicd-lab-007','L2','Debug a flaky test','A test fails intermittently and teams are retrying the pipeline.','Identify and remove the nondeterministic dependency.',['Collect repeated failures','Check timing/shared state/network','Isolate test','Fix synchronization/data cleanup','Remove excessive retry'],['./mvnw test -Dtest=ProblemTest'],'Retries are evidence collection, not the permanent solution.',['When is retry acceptable?','How do you quarantine safely?'],['Unlimited retries make CI untrustworthy.']),
lab('cicd-lab-008','L3','Deploy Spring Boot to Kubernetes from CI/CD','A tested Docker image needs automated deployment to Kubernetes.','Create a safe staging deployment with rollout verification.',['Build/test image','Scan/push','Update Deployment image','Wait for rollout','Run smoke test','Capture revision'],['kubectl set image deployment/api api=registry/api:$GIT_SHA','kubectl rollout status deployment/api'],'The pipeline should fail if Kubernetes accepts the manifest but the application never becomes ready.',['Where do deployment credentials come from?','How do you rollback?'],['Do not give the CI job cluster-admin.']),
lab('cicd-lab-009','L3','Implement canary promotion logic','A new release may have unknown production behavior.','Route a small percentage of traffic and promote only when health signals remain acceptable.',['Deploy candidate','Route small traffic','Observe error/latency/business metrics','Pause or promote','Rollback on degradation'],['# conceptual: 5% → 25% → 50% → 100%'],'Canary is a controlled experiment with explicit abort criteria.',['Which metrics?','How long should each stage observe?'],['Do not use traffic percentage without meaningful health gates.']),
lab('cicd-lab-010','L3','Design a production-grade CI/CD platform','Multiple React/Spring Boot services deploy to Kubernetes.','Design a secure, traceable and observable delivery platform.',['PR validation','Protected main','Build once','SCA/SAST/container scan','Registry','Staging promotion','Progressive production','Telemetry and rollback'],['Git → CI → registry → Kubernetes → observability'],'I would standardize reusable pipeline components while preserving service-level independent deployment.',['How do you handle DB migrations?','How do you secure runners?'],['Do not create one giant pipeline that couples every microservice release.']),
];
