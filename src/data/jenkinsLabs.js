const lab=(id,level,title,situation,goal,steps,commands,delivery,followups,followupAnswers,traps)=>({id,level,title,situation,goal,steps,commands,delivery,followups,followupAnswers,traps});
export const jenkinsLabs=[
lab('jenkins-lab-001','L1','Create a Jenkinsfile for Spring Boot','A Spring Boot service has manual builds and no CI pipeline.','Create a Declarative Jenkinsfile that checks out the revision, runs tests, packages the JAR and publishes test evidence.',["Create Jenkinsfile", "Use Maven wrapper", "Run tests", "Package artifact", "Publish JUnit evidence"],["pipeline {", "  agent any", "  stages {", "    stage(\"Test\") { steps { sh \"./mvnw test\" } }", "    stage(\"Package\") { steps { sh \"./mvnw package -DskipTests\" } }", "  }", "}"],'I would start with deterministic validation before adding deployment complexity.',["What happens when tests fail?", "Why use the Maven wrapper?"],["The test stage returns a non-zero status, the pipeline should stop before packaging or deployment, and JUnit evidence should still be published in post/always handling. Production credentials and promotion stages should never become reachable just because later cleanup runs.", "It makes the Maven version controlled by the repository, so CI is less dependent on whatever Maven happens to be installed on the agent. I still pin the JDK and agent image for full reproducibility."],["Do not make deployment the first pipeline milestone."]),
lab('jenkins-lab-002','L1','Build a Multibranch PR pipeline','A repository has many feature branches and PRs.','Configure branch-aware CI so each revision is validated using the repository Jenkinsfile.',["Connect SCM", "Enable branch/PR discovery", "Run Jenkinsfile from each revision", "Publish checks", "Protect main with required checks"],["# conceptual", "SCM webhook \u2192 multibranch scan \u2192 branch job \u2192 Jenkinsfile"],'Multibranch gives each branch its own CI context without copying job definitions.',["How do fork PRs affect secrets?", "How do you stop production deploys?"],["Fork PR code must be treated as untrusted because it can alter build scripts. I run it without production secrets or privileged deployment access and use a separate trusted release path after merge.", "Gate production deployment on the trusted branch/release job, required checks, authorization, and explicit approval. A PR pipeline should not merely hide the deploy stage; it should lack the credentials and permissions needed to perform it."],["Never expose production credentials to untrusted PR execution."]),
lab('jenkins-lab-003','L2','Build a Docker publish pipeline','A validated Spring Boot JAR must become an immutable image.','Build, scan and publish an image tagged by Git SHA, then record its digest.',["Run application tests", "Build image", "Scan candidate", "Login using Jenkins credentials", "Push image", "Record digest"],["docker build -t registry/orders:$GIT_COMMIT .", "docker push registry/orders:$GIT_COMMIT", "docker inspect --format=\"{{index .RepoDigests 0}}\" registry/orders:$GIT_COMMIT"],'The image identity should be traceable to the exact commit and digest.',["Why not latest?", "Where should registry credentials live?"],["latest is mutable, so it does not prove which content was deployed and can change between environments. I tag with a commit/release identifier and record the immutable digest for promotion.", "Store them in Jenkins Credentials or an external secret/identity system and bind them only during the publish step. The pipeline should never commit or echo registry passwords."],["Do not publish an image before required security gates pass."]),
lab('jenkins-lab-004','L2','Add parallel quality checks','The pipeline is slow because independent checks run sequentially.','Run unit tests, lint/static checks and dependency checks concurrently where safe.',["Identify independent stages", "Create parallel block", "Keep shared inputs immutable", "Collect all results", "Gate on required failures"],["parallel {", "  stage(\"Unit\") { steps { sh \"./mvnw test\" } }", "  stage(\"Static\") { steps { sh \"./mvnw verify\" } }", "}"],'Parallelism should reduce critical-path time without introducing workspace races.',["What if two stages modify the workspace?", "How do you collect failures?"],["Give them isolated workspaces/agents or make the inputs read-only and outputs separate. Parallel stages should not race over the same generated files because that makes failures nondeterministic.", "Let independent stages finish so all required failures are visible, then fail the quality gate based on their results. Publish each test/static-analysis report even when another parallel branch fails."],["Do not parallelize conflicting writes to the same workspace."]),
lab('jenkins-lab-005','L2','Implement a protected production approval','Production deployment must require an explicit authorized approval.','Automate all evidence first and pause only at the production risk boundary.',["Deploy staging", "Run smoke tests", "Publish evidence", "Request approval", "Deploy production", "Verify health"],["input message: \"Promote validated release?\"", "kubectl rollout status deployment/orders"],'The approver should see test, scan and staging evidence before approving.',["Who can approve?", "Can approval be skipped?"],["Only an authorized release group or identity should approve production, with permissions separate from ordinary CI execution. The approval record should identify the person, release, and evidence they approved.", "Not through a parameter or Jenkinsfile branch that an untrusted contributor can modify. The production job and its permissions should enforce the approval requirement outside the developer-controlled path."],["Manual SSH outside Jenkins breaks auditability."]),
lab('jenkins-lab-006','L2','Debug a Jenkins environment mismatch','Local Maven builds pass but the Jenkins agent fails.','Find the environmental difference and make the build reproducible.',["Compare versions", "Inspect workspace", "Check lockfiles/wrapper", "Check permissions/network", "Reproduce with agent image", "Fix root cause"],["java -version", "./mvnw -version", "env | sort"],'I compare the runner to the local environment instead of immediately changing application code.',["What should be pinned?", "When is retry useful?"],["Pin the JDK, Maven/Node versions, agent image, dependency lockfiles, action/plugin/tool versions where practical, and deployment tooling. Pinning the environment makes a CI failure reproducible instead of dependent on whatever happens to be installed today.", "Retry only operations with a known transient failure mode, such as a temporary registry/network error. Do not use retry to hide deterministic build, test, configuration, or authorization failures."],["Retries do not fix deterministic environment mismatches."]),
lab('jenkins-lab-007','L2','Debug an executor queue problem','A release job has been waiting for an executor for 20 minutes.','Determine whether labels, capacity or concurrency controls are causing the queue delay.',["Inspect queue reason", "Check matching labels", "Inspect executor usage", "Check disabled/offline agents", "Adjust capacity or scheduling"],["# Jenkins UI: Build Queue", "# inspect node labels/executors"],'Queue diagnosis starts with why Jenkins says the build cannot be scheduled.',["Would more executors help?", "What if all agents are offline?"],["Only if the queue is caused by insufficient executor capacity and the node has enough CPU/memory to support more concurrent work. If agents are offline, labels do not match, or a downstream resource is saturated, more executors will not solve the root cause.", "Check the provisioning/controller connectivity, agent launch logs, credentials, network/DNS, capacity limits, and underlying Kubernetes/cloud infrastructure. Fix the agent platform first rather than modifying application pipeline steps."],["Do not increase executors on a resource-starved node blindly."]),
lab('jenkins-lab-008','L3','Deploy an immutable image to Kubernetes','The image is validated and stored in a registry.','Have Jenkins update Kubernetes to the exact release and verify rollout health.',["Authenticate with scoped identity", "Set image by immutable version", "Wait for rollout", "Run smoke test", "Record revision"],["kubectl set image deployment/orders orders=registry/orders:$GIT_COMMIT", "kubectl rollout status deployment/orders --timeout=180s"],'The pipeline should fail if the workload never becomes ready.',["How do you rollback?", "How do you scope cluster access?"],["Use the Kubernetes Deployment revision or redeploy the previous immutable image digest, then wait for rollout and verify health. Keep the rollback tied to the release evidence so the previous version is unambiguous.", "Create a dedicated service account or workload identity with only the verbs/resources/namespaces needed by the deployment. Avoid cluster-admin and separate production deploy identity from PR/CI identities."],["Do not use cluster-admin for the pipeline."]),
lab('jenkins-lab-009','L3','Implement canary promotion gates','A production release should start with a small traffic slice.','Automate staged promotion based on measurable health signals.',["Deploy candidate", "Route small traffic", "Observe error/latency/business metrics", "Pause on breach", "Promote or rollback"],["# conceptual", "5% \u2192 25% \u2192 50% \u2192 100%"],'Canary promotion is a sequence of controlled decisions, not a single deployment command.',["Which metrics are mandatory?", "How long should each stage observe?"],["At minimum I want error rate, latency, saturation/resource health, restarts, and rollout readiness, plus a business metric when the service has a meaningful success signal. The actual thresholds should come from the service SLO and failure impact.", "Observe long enough to collect representative traffic for that service, using either a time window or request count. Low-traffic services need longer windows or synthetic traffic so a canary cannot pass simply because nothing happened."],["Do not promote solely because pods are Ready."]),
lab('jenkins-lab-010','L3','Secure a Jenkins release path','A pipeline can currently access too many credentials and cluster permissions.','Redesign trust boundaries so PR builds cannot deploy production.',["Separate PR and release jobs", "Use ephemeral agents", "Scope credentials", "Use protected approval", "Audit deployments", "Rotate identities"],["PR agent \u2192 no production credential", "release agent \u2192 scoped deploy identity"],'I minimize privilege at the pipeline, agent and credential levels.',["How do you handle fork PRs?", "Why ephemeral agents?"],["Run fork code in an isolated, untrusted CI context with no production credentials and minimal network access. Only the trusted post-merge release path receives deployment permissions.", "They provide a clean, known environment per build and reduce persistent state and compromise across jobs. This is especially valuable for release pipelines handling credentials or sensitive artifacts."],["Credential masking does not prevent an authorized pipeline from abusing a secret."]),
lab('jenkins-lab-011','L3','Build a production release evidence trail','Auditors need to know what commit became which production image and deployment.','Record commit SHA, Jenkins build, artifact digest, scan/SBOM results, approval and Kubernetes revision.',["Capture SCM revision", "Record image digest", "Attach scan/SBOM evidence", "Record approval", "Record deployment revision", "Publish release summary"],["GIT_COMMIT \u2192 BUILD_NUMBER \u2192 IMAGE_DIGEST \u2192 K8S_REVISION"],'Traceability turns a deployment into an auditable chain of evidence.',["What must be immutable?", "How does this help rollback?"],["The production release identity should include the source commit and artifact/image digests; mutable tags alone are insufficient. Evidence should preserve those identities so the exact deployed content can always be reconstructed.", "A complete evidence trail tells us exactly which previous image, commit, and deployment revision was known good. Rollback can then select that immutable release instead of guessing from a mutable tag."],["Do not depend on mutable tags as the only release record."]),
lab('jenkins-lab-012','L3','Design the complete Jenkins platform','A company has React frontends and Spring Boot microservices deploying to Kubernetes.','Design a secure, scalable Jenkins platform with reusable pipelines and progressive delivery.',["PR validation", "Shared library standards", "Ephemeral agents", "Build/test/scan", "Immutable registry", "Staging promotion", "Progressive production", "Observability/rollback"],["SCM \u2192 Jenkins \u2192 ephemeral agents \u2192 registry \u2192 Kubernetes \u2192 telemetry", "PR identity \u2260 production deploy identity"],'The platform should standardize controls while keeping services independently deployable.',["How do you scale agents?", "How do you handle database migrations?"],["Provision ephemeral agents from a versioned image and scale them based on queue demand while enforcing CPU/memory and concurrency limits. Kubernetes-based Jenkins agents are a common way to make this elastic.", "Run migrations as a controlled, versioned release step using a migration tool and a least-privileged database identity. Prefer backward-compatible expand/contract changes so application rollback remains possible without requiring an unsafe schema rollback."],["Do not create one giant pipeline coupling every service release."]),
];
