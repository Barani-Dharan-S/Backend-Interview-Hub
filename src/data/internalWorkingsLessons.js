const lesson=(id,level,topic,title,what,how,code,delivery,followups,answers,traps,realWorld)=>({id,level,topic,title,what,how,code,delivery,followups,followupAnswers:answers,traps,realWorld});

export const internalWorkingsLessons=[
lesson("iw-001","L2","Java / JVM","Class loading","Class loading turns bytecode into a usable runtime type.","The JVM locates a class, loads its bytes, verifies and prepares it, then initializes it when required. Parent delegation prevents ordinary loaders from redefining core classes accidentally.","Class.forName(\"com.example.Payment\");","I separate loading, linking and initialization; that distinction explains many NoClassDefFoundError and initialization failures.",["What failure does class loading help you diagnose?", "What trade-off should you consider with class loading?"],["I would inspect JVM evidence that matches the mechanism: GC logs and heap data for memory behavior, thread dumps for blocked threads, JIT/CPU profiling for hot code, and thread contention data for concurrency. I would validate the hypothesis against the observed workload.", "The trade-off is runtime efficiency versus predictability and resource cost. JVM optimizations can reduce CPU or allocations, but they can also change warm-up, memory pressure or latency characteristics, so I verify with production-like measurements."],["Do not describe class loading as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-002","L2","Java / JVM","Stack vs heap","Each thread has its own stack frames while objects normally live in the heap.","A method call creates a frame containing locals and operand state; object allocation is managed by the JVM heap and collector. Escape analysis may optimize some allocations.","void pay(){ int amount=10; Payment p=new Payment(); }","I use stack and heap as a mental model, then qualify it: the JVM is free to optimize representation.",["What failure does stack vs heap help you diagnose?", "What trade-off should you consider with stack vs heap?"],["I would inspect JVM evidence that matches the mechanism: GC logs and heap data for memory behavior, thread dumps for blocked threads, JIT/CPU profiling for hot code, and thread contention data for concurrency. I would validate the hypothesis against the observed workload.", "The trade-off is runtime efficiency versus predictability and resource cost. JVM optimizations can reduce CPU or allocations, but they can also change warm-up, memory pressure or latency characteristics, so I verify with production-like measurements."],["Do not describe stack vs heap as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-003","L2","Java / JVM","JIT compilation","The JIT turns frequently executed bytecode into optimized native machine code at runtime.","The JVM profiles execution, identifies hot methods, compiles them and can apply inlining and other speculative optimizations.","for(int i=0;i<1_000_000;i++) total += calculate(i);","I explain why warm-up and workload shape can affect observed Java performance.",["What failure does jit compilation help you diagnose?", "What trade-off should you consider with jit compilation?"],["I would inspect JVM evidence that matches the mechanism: GC logs and heap data for memory behavior, thread dumps for blocked threads, JIT/CPU profiling for hot code, and thread contention data for concurrency. I would validate the hypothesis against the observed workload.", "The trade-off is runtime efficiency versus predictability and resource cost. JVM optimizations can reduce CPU or allocations, but they can also change warm-up, memory pressure or latency characteristics, so I verify with production-like measurements."],["Do not describe jit compilation as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-004","L2","Java / JVM","Safepoints","A safepoint is a JVM coordination point where threads can reach a state safe for certain VM operations.","Threads move through poll points and the JVM waits for required threads before operations such as some GC phases or deoptimization.","-XX:+PrintGCDetails","I avoid saying every GC is a full stop-the-world event; I explain the collector and safepoint behavior involved.",["What failure does safepoints help you diagnose?", "What trade-off should you consider with safepoints?"],["I would inspect JVM evidence that matches the mechanism: GC logs and heap data for memory behavior, thread dumps for blocked threads, JIT/CPU profiling for hot code, and thread contention data for concurrency. I would validate the hypothesis against the observed workload.", "The trade-off is runtime efficiency versus predictability and resource cost. JVM optimizations can reduce CPU or allocations, but they can also change warm-up, memory pressure or latency characteristics, so I verify with production-like measurements."],["Do not describe safepoints as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-005","L2","Java / JVM","Java Memory Model","The Java Memory Model defines when writes by one thread become visible to another and what reorderings are legal.","Happens-before relationships arise from synchronization, volatile access, thread start/join and other rules.","volatile boolean ready;","I reason from happens-before rather than from source-code order alone.",["What failure does java memory model help you diagnose?", "What trade-off should you consider with java memory model?"],["I would inspect JVM evidence that matches the mechanism: GC logs and heap data for memory behavior, thread dumps for blocked threads, JIT/CPU profiling for hot code, and thread contention data for concurrency. I would validate the hypothesis against the observed workload.", "The trade-off is runtime efficiency versus predictability and resource cost. JVM optimizations can reduce CPU or allocations, but they can also change warm-up, memory pressure or latency characteristics, so I verify with production-like measurements."],["Do not describe java memory model as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-006","L2","Java / JVM","Thread pools","A thread pool bounds and reuses worker threads for submitted tasks.","Tasks enter a queue, workers execute them, and saturation depends on queue policy, worker count and task duration.","ExecutorService pool = Executors.newFixedThreadPool(8);","I size pools from workload characteristics and downstream capacity instead of choosing a number by habit.",["What failure does thread pools help you diagnose?", "What trade-off should you consider with thread pools?"],["I would inspect JVM evidence that matches the mechanism: GC logs and heap data for memory behavior, thread dumps for blocked threads, JIT/CPU profiling for hot code, and thread contention data for concurrency. I would validate the hypothesis against the observed workload.", "The trade-off is runtime efficiency versus predictability and resource cost. JVM optimizations can reduce CPU or allocations, but they can also change warm-up, memory pressure or latency characteristics, so I verify with production-like measurements."],["Do not describe thread pools as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-007","L2","Spring Internals","Bean definition to bean instance","A Spring bean begins as metadata describing how the container should create and configure an object.","Bean definitions are discovered, dependencies resolved, instances created and post-processors applied before the bean is exposed.","@Bean PaymentService paymentService(){ return new PaymentService(); }","I describe the container as a lifecycle pipeline, which makes startup and wiring problems easier to reason about.",["What failure does bean definition to bean instance help you diagnose?", "What trade-off should you consider with bean definition to bean instance?"],["I would trace the application-context or proxy boundary involved, enable focused Spring logs, inspect the bean or interceptor that owns the behavior, and compare an invocation that crosses the container with one that does not.", "The consequence is usually startup failure, unexpected proxy behavior, ordering surprises or hidden infrastructure work. I keep the configuration explicit and verify which bean or interceptor actually owns the behavior."],["Do not describe bean definition to bean instance as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-008","L2","Spring Internals","Dependency injection resolution","Dependency injection resolves a bean dependency from the application context rather than the class constructing it itself.","Spring considers type, qualifiers, primary candidates and injection metadata when selecting a dependency.","@Qualifier(\"stripe\") PaymentGateway gateway;","I explain why constructor injection plus explicit qualifiers makes dependency selection predictable.",["What failure does dependency injection resolution help you diagnose?", "What trade-off should you consider with dependency injection resolution?"],["I would trace the application-context or proxy boundary involved, enable focused Spring logs, inspect the bean or interceptor that owns the behavior, and compare an invocation that crosses the container with one that does not.", "The consequence is usually startup failure, unexpected proxy behavior, ordering surprises or hidden infrastructure work. I keep the configuration explicit and verify which bean or interceptor actually owns the behavior."],["Do not describe dependency injection resolution as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-009","L2","Spring Internals","Bean post-processors","BeanPostProcessors can inspect, modify or wrap beans during container creation.","Before/after initialization callbacks allow infrastructure such as AOP and annotation-driven features to participate in bean creation.","class AuditProcessor implements BeanPostProcessor { }","I use post-processors to explain why a bean may be proxied or behave differently after creation.",["What failure does bean post-processors help you diagnose?", "What trade-off should you consider with bean post-processors?"],["I would trace the application-context or proxy boundary involved, enable focused Spring logs, inspect the bean or interceptor that owns the behavior, and compare an invocation that crosses the container with one that does not.", "The consequence is usually startup failure, unexpected proxy behavior, ordering surprises or hidden infrastructure work. I keep the configuration explicit and verify which bean or interceptor actually owns the behavior."],["Do not describe bean post-processors as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-010","L2","Spring Internals","Spring proxies","Spring proxies intercept calls to apply cross-cutting behavior such as transactions, caching or security.","JDK proxies implement interfaces; subclass-based proxies extend concrete classes. Calls must cross the proxy for advice to run.","proxy.payment(); // advice can run here","I always identify the proxy boundary before explaining why an annotation did or did not take effect.",["What failure does spring proxies help you diagnose?", "What trade-off should you consider with spring proxies?"],["I would trace the application-context or proxy boundary involved, enable focused Spring logs, inspect the bean or interceptor that owns the behavior, and compare an invocation that crosses the container with one that does not.", "The consequence is usually startup failure, unexpected proxy behavior, ordering surprises or hidden infrastructure work. I keep the configuration explicit and verify which bean or interceptor actually owns the behavior."],["Do not describe spring proxies as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-011","L2","Spring Internals","Application startup","Spring Boot startup builds the application context and prepares infrastructure before serving requests.","Configuration is discovered, auto-configuration is evaluated, beans are created and lifecycle callbacks run.","SpringApplication.run(App.class,args);","I troubleshoot startup by locating the first failing bean or configuration boundary, not the last stack-trace line.",["What failure does application startup help you diagnose?", "What trade-off should you consider with application startup?"],["I would trace the application-context or proxy boundary involved, enable focused Spring logs, inspect the bean or interceptor that owns the behavior, and compare an invocation that crosses the container with one that does not.", "The consequence is usually startup failure, unexpected proxy behavior, ordering surprises or hidden infrastructure work. I keep the configuration explicit and verify which bean or interceptor actually owns the behavior."],["Do not describe application startup as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-012","L2","Spring Internals","Auto-configuration conditions","Boot auto-configuration adds infrastructure when classpath, properties and existing beans satisfy conditions.","Conditional annotations decide whether a configuration contributes beans, allowing sensible defaults without overriding explicit application configuration.","@ConditionalOnClass(DataSource.class)","I explain auto-configuration as conditional defaults rather than hidden magic.",["What failure does auto-configuration conditions help you diagnose?", "What trade-off should you consider with auto-configuration conditions?"],["I would trace the application-context or proxy boundary involved, enable focused Spring logs, inspect the bean or interceptor that owns the behavior, and compare an invocation that crosses the container with one that does not.", "The consequence is usually startup failure, unexpected proxy behavior, ordering surprises or hidden infrastructure work. I keep the configuration explicit and verify which bean or interceptor actually owns the behavior."],["Do not describe auto-configuration conditions as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-013","L2","Spring Web / Security","Filter chain ordering","HTTP filters execute around the servlet request and their order determines which concerns run first.","Spring Security builds a filter chain that can authenticate, authorize, reject or continue the request before MVC controller invocation.","http.addFilterBefore(customFilter, UsernamePasswordAuthenticationFilter.class);","I diagnose security behavior by tracing the filter chain before looking at the controller.",["What failure does filter chain ordering help you diagnose?", "What trade-off should you consider with filter chain ordering?"],["I would trace the request from the security filter chain through DispatcherServlet, handler mapping, message conversion and controller advice, checking the first boundary that rejects or transforms the request.", "The common mistake is debugging only the controller. Authentication, CORS, content negotiation and exception translation can reject or transform a request before business logic runs, so I identify the owning layer first."],["Do not describe filter chain ordering as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-014","L2","Spring Web / Security","Handler mapping","Spring MVC maps an incoming request to a controller handler method.","DispatcherServlet asks HandlerMappings for a matching handler based on path, method and other conditions.","GET /orders/42 -> OrderController#get(42)","I use handler mapping to explain 404 versus 405 and why a request never reached business logic.",["What failure does handler mapping help you diagnose?", "What trade-off should you consider with handler mapping?"],["I would trace the request from the security filter chain through DispatcherServlet, handler mapping, message conversion and controller advice, checking the first boundary that rejects or transforms the request.", "The common mistake is debugging only the controller. Authentication, CORS, content negotiation and exception translation can reject or transform a request before business logic runs, so I identify the owning layer first."],["Do not describe handler mapping as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-015","L2","Spring Web / Security","Message conversion","Spring converts HTTP representations such as JSON into Java objects and back.","HttpMessageConverters inspect content type and use serializers such as Jackson for request and response bodies.","OrderRequest req // JSON -> object","I distinguish transport serialization from validation and business rules.",["What failure does message conversion help you diagnose?", "What trade-off should you consider with message conversion?"],["I would trace the request from the security filter chain through DispatcherServlet, handler mapping, message conversion and controller advice, checking the first boundary that rejects or transforms the request.", "The common mistake is debugging only the controller. Authentication, CORS, content negotiation and exception translation can reject or transform a request before business logic runs, so I identify the owning layer first."],["Do not describe message conversion as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-016","L2","Spring Web / Security","JWT validation","JWT validation proves that a token is structurally and cryptographically acceptable before its claims are trusted.","The resource server validates signature, issuer, audience and time claims according to configuration, then creates an authenticated principal.","Authorization: Bearer eyJ...","I never treat decoding a JWT as validation; trust begins only after verification succeeds.",["What failure does jwt validation help you diagnose?", "What trade-off should you consider with jwt validation?"],["I would trace the request from the security filter chain through DispatcherServlet, handler mapping, message conversion and controller advice, checking the first boundary that rejects or transforms the request.", "The common mistake is debugging only the controller. Authentication, CORS, content negotiation and exception translation can reject or transform a request before business logic runs, so I identify the owning layer first."],["Do not describe jwt validation as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-017","L2","Spring Web / Security","Authentication vs authorization","Authentication establishes who the caller is; authorization decides what that caller may do.","Security filters can establish an authenticated principal, while authorization rules evaluate authorities against the requested resource.","@PreAuthorize(\"hasRole('ADMIN')\")","I explain the two decisions separately because a valid identity does not imply permission.",["What failure does authentication vs authorization help you diagnose?", "What trade-off should you consider with authentication vs authorization?"],["I would trace the request from the security filter chain through DispatcherServlet, handler mapping, message conversion and controller advice, checking the first boundary that rejects or transforms the request.", "The common mistake is debugging only the controller. Authentication, CORS, content negotiation and exception translation can reject or transform a request before business logic runs, so I identify the owning layer first."],["Do not describe authentication vs authorization as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-018","L2","Spring Web / Security","Exception translation","Security and MVC translate internal failures into stable HTTP responses.","Authentication failures commonly become 401, access denial becomes 403, and controller errors can be centralized with exception handlers.","@RestControllerAdvice class Errors { }","I trace the boundary that owns the response instead of returning ad-hoc errors from every layer.",["What failure does exception translation help you diagnose?", "What trade-off should you consider with exception translation?"],["I would trace the request from the security filter chain through DispatcherServlet, handler mapping, message conversion and controller advice, checking the first boundary that rejects or transforms the request.", "The common mistake is debugging only the controller. Authentication, CORS, content negotiation and exception translation can reject or transform a request before business logic runs, so I identify the owning layer first."],["Do not describe exception translation as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-019","L2","JPA / Hibernate","Persistence context","The persistence context is Hibernate’s managed identity scope for entities.","Within one context, a database row is represented by one managed entity instance; changes can be tracked until flush.","Order a = em.find(Order.class,1L); Order b = em.find(Order.class,1L);","I connect persistence context, identity map and dirty checking rather than treating JPA as a simple SQL wrapper.",["What failure does persistence context help you diagnose?", "What trade-off should you consider with persistence context?"],["I would enable SQL and bind-parameter logging, inspect transaction/flush boundaries, and compare the entity access pattern with the generated SQL. For performance problems I would also inspect the database execution plan.", "Fetch joins, batching and larger persistence contexts can reduce round trips but increase result-set size or memory usage. I choose a fetch plan from the endpoint's data shape rather than applying eager loading everywhere."],["Do not describe persistence context as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-020","L2","JPA / Hibernate","Entity state transitions","Entities move among transient, managed, detached and removed states.","persist attaches new state, detach/clear removes management, merge copies detached state into a managed instance, and remove schedules deletion.","em.persist(order); em.flush();","I choose the operation based on entity state instead of using merge blindly.",["What failure does entity state transitions help you diagnose?", "What trade-off should you consider with entity state transitions?"],["I would enable SQL and bind-parameter logging, inspect transaction/flush boundaries, and compare the entity access pattern with the generated SQL. For performance problems I would also inspect the database execution plan.", "Fetch joins, batching and larger persistence contexts can reduce round trips but increase result-set size or memory usage. I choose a fetch plan from the endpoint's data shape rather than applying eager loading everywhere."],["Do not describe entity state transitions as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-021","L2","JPA / Hibernate","Dirty checking","Dirty checking detects changes to managed entity state and synchronizes them during flush.","Hibernate keeps enough state to determine what changed and generates SQL for dirty managed entities.","order.setStatus(\"PAID\"); // SQL later","I explain why a setter can cause an UPDATE without an explicit repository save.",["What failure does dirty checking help you diagnose?", "What trade-off should you consider with dirty checking?"],["I would enable SQL and bind-parameter logging, inspect transaction/flush boundaries, and compare the entity access pattern with the generated SQL. For performance problems I would also inspect the database execution plan.", "Fetch joins, batching and larger persistence contexts can reduce round trips but increase result-set size or memory usage. I choose a fetch plan from the endpoint's data shape rather than applying eager loading everywhere."],["Do not describe dirty checking as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-022","L2","JPA / Hibernate","Flush vs commit","Flush synchronizes persistence context state with the database; commit completes the transaction.","A flush can happen before commit, including before queries under some modes, while commit finalizes the transaction.","entityManager.flush();","I never equate flush with commit; visibility and durability still depend on transaction semantics.",["What failure does flush vs commit help you diagnose?", "What trade-off should you consider with flush vs commit?"],["I would enable SQL and bind-parameter logging, inspect transaction/flush boundaries, and compare the entity access pattern with the generated SQL. For performance problems I would also inspect the database execution plan.", "Fetch joins, batching and larger persistence contexts can reduce round trips but increase result-set size or memory usage. I choose a fetch plan from the endpoint's data shape rather than applying eager loading everywhere."],["Do not describe flush vs commit as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-023","L2","JPA / Hibernate","Lazy loading","Lazy loading defers association retrieval until the association is accessed.","Hibernate can use proxies or enhanced state to issue a query when a lazy relationship is first needed.","order.getItems().size(); // may query","I make fetch strategy an explicit API and transaction-boundary decision rather than exposing lazy entities blindly.",["What failure does lazy loading help you diagnose?", "What trade-off should you consider with lazy loading?"],["I would enable SQL and bind-parameter logging, inspect transaction/flush boundaries, and compare the entity access pattern with the generated SQL. For performance problems I would also inspect the database execution plan.", "Fetch joins, batching and larger persistence contexts can reduce round trips but increase result-set size or memory usage. I choose a fetch plan from the endpoint's data shape rather than applying eager loading everywhere."],["Do not describe lazy loading as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-024","L2","JPA / Hibernate","N+1 query mechanism","N+1 occurs when one query loads parent rows and repeated lazy access triggers one query per parent.","The first query returns N parents; iteration then initializes each association separately. Fetch joins, entity graphs or batch fetching can reduce round trips.","SELECT o FROM Order o JOIN FETCH o.items","I identify N+1 from query counts and request shape, then choose the smallest correct fetch strategy.",["What failure does n+1 query mechanism help you diagnose?", "What trade-off should you consider with n+1 query mechanism?"],["I would enable SQL and bind-parameter logging, inspect transaction/flush boundaries, and compare the entity access pattern with the generated SQL. For performance problems I would also inspect the database execution plan.", "Fetch joins, batching and larger persistence contexts can reduce round trips but increase result-set size or memory usage. I choose a fetch plan from the endpoint's data shape rather than applying eager loading everywhere."],["Do not describe n+1 query mechanism as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-025","L2","SQL / PostgreSQL","B-tree index lookup","A B-tree index keeps keys ordered so the engine can navigate to matching ranges efficiently.","The planner can use the index to find a narrow key range and then fetch table rows or perform an index-only strategy when possible.","CREATE INDEX idx_orders_customer ON orders(customer_id);","I explain an index as an access path with a cost, not as a magic speed switch.",["What failure does b-tree index lookup help you diagnose?", "What trade-off should you consider with b-tree index lookup?"],["I would start with EXPLAIN (ANALYZE, BUFFERS), compare estimated versus actual rows, inspect scan and join choices, and check locks or connection state when the issue is concurrency-related.", "An index or stronger isolation can improve one workload while increasing write cost, storage, locking or contention. I choose based on the invariant and workload, then validate with measured plans and timings."],["Do not describe b-tree index lookup as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-026","L2","SQL / PostgreSQL","Query planning","The optimizer chooses an execution plan using statistics, predicates, indexes, joins and estimated costs.","EXPLAIN reveals scans, joins, row estimates and actual work when EXPLAIN ANALYZE is used.","EXPLAIN (ANALYZE, BUFFERS) SELECT ...;","I compare estimated versus actual rows first because bad cardinality estimates often lead to bad plans.",["What failure does query planning help you diagnose?", "What trade-off should you consider with query planning?"],["I would start with EXPLAIN (ANALYZE, BUFFERS), compare estimated versus actual rows, inspect scan and join choices, and check locks or connection state when the issue is concurrency-related.", "An index or stronger isolation can improve one workload while increasing write cost, storage, locking or contention. I choose based on the invariant and workload, then validate with measured plans and timings."],["Do not describe query planning as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-027","L2","SQL / PostgreSQL","MVCC","Multi-version concurrency control lets transactions work with consistent row versions while reducing reader/writer blocking.","PostgreSQL tracks row versions and transaction visibility so a query sees rows valid for its snapshot.","SELECT txid_current();","I explain MVCC through snapshots and row versions, then connect it to vacuum and bloat.",["What failure does mvcc help you diagnose?", "What trade-off should you consider with mvcc?"],["I would start with EXPLAIN (ANALYZE, BUFFERS), compare estimated versus actual rows, inspect scan and join choices, and check locks or connection state when the issue is concurrency-related.", "An index or stronger isolation can improve one workload while increasing write cost, storage, locking or contention. I choose based on the invariant and workload, then validate with measured plans and timings."],["Do not describe mvcc as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-028","L2","SQL / PostgreSQL","Locking","Database locks coordinate conflicting operations so concurrent transactions preserve required invariants.","Row and table locks can block or conflict; transaction design determines how long locks are held and whether deadlocks can occur.","SELECT * FROM orders WHERE id=1 FOR UPDATE;","I diagnose lock waits by identifying the blocked statement and the transaction holding the conflicting lock.",["What failure does locking help you diagnose?", "What trade-off should you consider with locking?"],["I would start with EXPLAIN (ANALYZE, BUFFERS), compare estimated versus actual rows, inspect scan and join choices, and check locks or connection state when the issue is concurrency-related.", "An index or stronger isolation can improve one workload while increasing write cost, storage, locking or contention. I choose based on the invariant and workload, then validate with measured plans and timings."],["Do not describe locking as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-029","L2","SQL / PostgreSQL","Isolation levels","Isolation controls which effects of concurrent transactions can be observed.","PostgreSQL implements isolation using snapshots and locking behavior; stronger isolation can improve guarantees while increasing aborts or contention.","SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;","I select isolation from the invariant that must hold, not from a blanket “highest is best” rule.",["What failure does isolation levels help you diagnose?", "What trade-off should you consider with isolation levels?"],["I would start with EXPLAIN (ANALYZE, BUFFERS), compare estimated versus actual rows, inspect scan and join choices, and check locks or connection state when the issue is concurrency-related.", "An index or stronger isolation can improve one workload while increasing write cost, storage, locking or contention. I choose based on the invariant and workload, then validate with measured plans and timings."],["Do not describe isolation levels as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-030","L2","SQL / PostgreSQL","Connection pooling","A pool reuses database connections so requests do not repeatedly pay connection setup cost.","Borrowers acquire a connection, execute work and return it; saturation occurs when all connections are busy or leaked.","HikariDataSource ds;","I tune pool size with database capacity and request concurrency, because a bigger pool can overload the database.",["What failure does connection pooling help you diagnose?", "What trade-off should you consider with connection pooling?"],["I would start with EXPLAIN (ANALYZE, BUFFERS), compare estimated versus actual rows, inspect scan and join choices, and check locks or connection state when the issue is concurrency-related.", "An index or stronger isolation can improve one workload while increasing write cost, storage, locking or contention. I choose based on the invariant and workload, then validate with measured plans and timings."],["Do not describe connection pooling as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-031","L2","Microservices","Request path across services","A distributed request crosses process and network boundaries, so latency and failure can occur at every hop.","Service A calls B, B may call C or a database, and each hop adds timeout, serialization and resource costs.","A -> B -> C -> DB","I draw the request path first because it reveals where latency, retries and ownership actually live.",["What failure does request path across services help you diagnose?", "What trade-off should you consider with request path across services?"],["I would follow one request using a correlation or trace ID, record each hop's latency and timeout, and identify whether the bottleneck is the caller, network, dependency or downstream resource.", "Retries, synchronous calls and stronger consistency can improve correctness or availability but consume capacity and add latency. I bound retries, propagate time budgets and make side effects idempotent where repetition is possible."],["Do not describe request path across services as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-032","L2","Microservices","Timeout propagation","A timeout limits how long a caller waits for a downstream operation.","Timeouts should be bounded across the call chain so a slow dependency does not consume every caller thread indefinitely.","client.timeout(800);","I set timeouts from an end-to-end latency budget and ensure downstream timeouts do not exceed the caller’s remaining budget.",["What failure does timeout propagation help you diagnose?", "What trade-off should you consider with timeout propagation?"],["I would follow one request using a correlation or trace ID, record each hop's latency and timeout, and identify whether the bottleneck is the caller, network, dependency or downstream resource.", "Retries, synchronous calls and stronger consistency can improve correctness or availability but consume capacity and add latency. I bound retries, propagate time budgets and make side effects idempotent where repetition is possible."],["Do not describe timeout propagation as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-033","L2","Microservices","Retry amplification","Retries can multiply load when several layers retry the same failing dependency.","If each layer retries independently, one original request can create many downstream attempts and worsen an outage.","retry(3) * retry(3) = 9 attempts","I prefer one well-placed retry policy with backoff and jitter and only for operations that are safe to retry.",["What failure does retry amplification help you diagnose?", "What trade-off should you consider with retry amplification?"],["I would follow one request using a correlation or trace ID, record each hop's latency and timeout, and identify whether the bottleneck is the caller, network, dependency or downstream resource.", "Retries, synchronous calls and stronger consistency can improve correctness or availability but consume capacity and add latency. I bound retries, propagate time budgets and make side effects idempotent where repetition is possible."],["Do not describe retry amplification as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-034","L2","Microservices","Circuit breaker states","A circuit breaker stops repeatedly calling an unhealthy dependency after failures cross a threshold.","Closed permits traffic, open fails fast, and half-open probes recovery before restoring normal traffic.","breaker.execute(remoteCall);","I explain the breaker as a load-shedding mechanism, not a fix for the dependency itself.",["What failure does circuit breaker states help you diagnose?", "What trade-off should you consider with circuit breaker states?"],["I would follow one request using a correlation or trace ID, record each hop's latency and timeout, and identify whether the bottleneck is the caller, network, dependency or downstream resource.", "Retries, synchronous calls and stronger consistency can improve correctness or availability but consume capacity and add latency. I bound retries, propagate time budgets and make side effects idempotent where repetition is possible."],["Do not describe circuit breaker states as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-035","L2","Microservices","Distributed transaction boundary","A business operation spanning services cannot rely on one local database transaction.","Each service commits locally and coordination is achieved through patterns such as saga, outbox and compensating actions.","Order DB tx -> event -> Payment DB tx","I choose a distributed consistency pattern based on business invariants and acceptable temporary inconsistency.",["What failure does distributed transaction boundary help you diagnose?", "What trade-off should you consider with distributed transaction boundary?"],["I would follow one request using a correlation or trace ID, record each hop's latency and timeout, and identify whether the bottleneck is the caller, network, dependency or downstream resource.", "Retries, synchronous calls and stronger consistency can improve correctness or availability but consume capacity and add latency. I bound retries, propagate time budgets and make side effects idempotent where repetition is possible."],["Do not describe distributed transaction boundary as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-036","L2","Microservices","Idempotency at service boundary","Idempotency lets repeated requests produce one logical effect.","A stable idempotency key is stored with the operation result or state so duplicate attempts can be recognized safely.","POST /payments\nIdempotency-Key: 7f2...","I make the boundary idempotent before adding retries because retries are expected in distributed systems.",["What failure does idempotency at service boundary help you diagnose?", "What trade-off should you consider with idempotency at service boundary?"],["I would follow one request using a correlation or trace ID, record each hop's latency and timeout, and identify whether the bottleneck is the caller, network, dependency or downstream resource.", "Retries, synchronous calls and stronger consistency can improve correctness or availability but consume capacity and add latency. I bound retries, propagate time budgets and make side effects idempotent where repetition is possible."],["Do not describe idempotency at service boundary as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-037","L3","Kafka","Partition leadership","Each Kafka partition has a leader broker responsible for normal client reads and writes.","Followers replicate the partition log; leadership can move after broker failure so another replica serves the partition.","topic orders partition 3 -> leader broker-2","I explain availability and ordering at partition scope, not topic-wide.",["What failure does partition leadership help you diagnose?", "What trade-off should you consider with partition leadership?"],["I would inspect partition leadership, consumer-group assignment, per-partition lag, processing time, rebalance events and downstream latency rather than looking only at total topic traffic.", "Kafka's broker semantics do not automatically make an external business side effect exactly once. Offset timing, idempotency, transactions and the external database determine the end-to-end behavior."],["Do not describe partition leadership as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-038","L3","Kafka","Consumer group coordination","A consumer group divides partitions among its consumers so work is distributed.","Membership changes trigger rebalancing, which changes partition ownership and can pause processing.","group: orders-consumers\npartitions: 0,1,2","I reason about scaling from partition count because consumers cannot parallelize a partition beyond its ordering model.",["What failure does consumer group coordination help you diagnose?", "What trade-off should you consider with consumer group coordination?"],["I would inspect partition leadership, consumer-group assignment, per-partition lag, processing time, rebalance events and downstream latency rather than looking only at total topic traffic.", "Kafka's broker semantics do not automatically make an external business side effect exactly once. Offset timing, idempotency, transactions and the external database determine the end-to-end behavior."],["Do not describe consumer group coordination as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-039","L3","Kafka","Offsets","A consumer offset represents its position in a partition log.","Committing an offset records progress; the timing of processing versus commit determines at-least-once or possible loss behavior.","consumer.commitSync();","I tie offset commit to the side effect so recovery semantics are explicit.",["What failure does offsets help you diagnose?", "What trade-off should you consider with offsets?"],["I would inspect partition leadership, consumer-group assignment, per-partition lag, processing time, rebalance events and downstream latency rather than looking only at total topic traffic.", "Kafka's broker semantics do not automatically make an external business side effect exactly once. Offset timing, idempotency, transactions and the external database determine the end-to-end behavior."],["Do not describe offsets as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-040","L3","Kafka","Delivery semantics","Kafka applications can provide at-most-once, at-least-once or carefully designed effectively-once behavior.","The broker alone does not make an external database side effect exactly once; application idempotency and transactions determine end-to-end behavior.","process(record); commitOffset();","I distinguish Kafka’s guarantees from the guarantees of the whole business workflow.",["What failure does delivery semantics help you diagnose?", "What trade-off should you consider with delivery semantics?"],["I would inspect partition leadership, consumer-group assignment, per-partition lag, processing time, rebalance events and downstream latency rather than looking only at total topic traffic.", "Kafka's broker semantics do not automatically make an external business side effect exactly once. Offset timing, idempotency, transactions and the external database determine the end-to-end behavior."],["Do not describe delivery semantics as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-041","L3","Kafka","Log compaction","Compaction retains the latest value for keys while allowing older records to be removed over time.","The broker rewrites segments and preserves the latest record per key, making compacted topics useful for state reconstruction.","cleanup.policy=compact","I use compaction for changelog/state topics, not as a substitute for arbitrary event retention.",["What failure does log compaction help you diagnose?", "What trade-off should you consider with log compaction?"],["I would inspect partition leadership, consumer-group assignment, per-partition lag, processing time, rebalance events and downstream latency rather than looking only at total topic traffic.", "Kafka's broker semantics do not automatically make an external business side effect exactly once. Offset timing, idempotency, transactions and the external database determine the end-to-end behavior."],["Do not describe log compaction as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-042","L3","Kafka","Consumer lag","Consumer lag measures how far a consumer group is behind the latest available records.","Lag grows when production rate exceeds consumption capacity or when consumers are blocked by downstream work.","records-lag-max","I correlate lag with processing latency, partition skew and dependency saturation before simply adding consumers.",["What failure does consumer lag help you diagnose?", "What trade-off should you consider with consumer lag?"],["I would inspect partition leadership, consumer-group assignment, per-partition lag, processing time, rebalance events and downstream latency rather than looking only at total topic traffic.", "Kafka's broker semantics do not automatically make an external business side effect exactly once. Offset timing, idempotency, transactions and the external database determine the end-to-end behavior."],["Do not describe consumer lag as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-043","L3","React / Browser","React render phase","The render phase calculates what the UI should look like for current props and state.","React invokes components and builds a new element tree; rendering should remain free of imperative side effects.","function App(){ return <Orders/> }","I separate render from commit because many React performance and side-effect bugs come from mixing those phases.",["What failure does react render phase help you diagnose?", "What trade-off should you consider with react render phase?"],["I would add request IDs or timing markers, use the browser Network and Performance tools, and deliberately introduce latency to expose ordering or rendering behavior that is hidden on a fast local machine.", "More rendering work, synchronous JavaScript or aggressive client caching can trade simplicity for responsiveness or freshness. I protect the UI thread, cancel stale work and measure before optimizing."],["Do not describe react render phase as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-044","L3","React / Browser","React commit phase","The commit phase applies necessary changes to the host environment after reconciliation.","React updates DOM nodes and runs relevant layout/effect lifecycle work according to its scheduling model.","useEffect(()=>load(),[]);","I explain that a component can render without every render causing equivalent DOM work.",["What failure does react commit phase help you diagnose?", "What trade-off should you consider with react commit phase?"],["I would add request IDs or timing markers, use the browser Network and Performance tools, and deliberately introduce latency to expose ordering or rendering behavior that is hidden on a fast local machine.", "More rendering work, synchronous JavaScript or aggressive client caching can trade simplicity for responsiveness or freshness. I protect the UI thread, cancel stale work and measure before optimizing."],["Do not describe react commit phase as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-045","L3","React / Browser","Browser event loop","The browser coordinates JavaScript execution with tasks, microtasks, rendering and other browser work.","A task runs, microtasks drain, and the browser may render between turns; long synchronous work blocks input and painting.","queueMicrotask(()=>console.log(\"micro\"));","I use the event loop to predict ordering and explain UI freezes rather than saying JavaScript is simply single-threaded.",["What failure does browser event loop help you diagnose?", "What trade-off should you consider with browser event loop?"],["I would add request IDs or timing markers, use the browser Network and Performance tools, and deliberately introduce latency to expose ordering or rendering behavior that is hidden on a fast local machine.", "More rendering work, synchronous JavaScript or aggressive client caching can trade simplicity for responsiveness or freshness. I protect the UI thread, cancel stale work and measure before optimizing."],["Do not describe browser event loop as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-046","L3","React / Browser","Promise microtasks","Promise callbacks run in the microtask queue after the current JavaScript stack completes.","Microtasks are drained before the browser proceeds to the next task, so excessive chains can delay rendering.","Promise.resolve().then(()=>console.log(\"micro\"));","I use a concrete queue ordering example when explaining async JavaScript.",["What failure does promise microtasks help you diagnose?", "What trade-off should you consider with promise microtasks?"],["I would add request IDs or timing markers, use the browser Network and Performance tools, and deliberately introduce latency to expose ordering or rendering behavior that is hidden on a fast local machine.", "More rendering work, synchronous JavaScript or aggressive client caching can trade simplicity for responsiveness or freshness. I protect the UI thread, cancel stale work and measure before optimizing."],["Do not describe promise microtasks as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-047","L3","React / Browser","React state batching","React can group state updates so multiple changes result in fewer renders.","Updates are queued and processed according to React’s scheduling rules; functional updates are important when deriving the next state from previous state.","setCount(c=>c+1); setCount(c=>c+1);","I use functional updates when several updates depend on prior state.",["What failure does react state batching help you diagnose?", "What trade-off should you consider with react state batching?"],["I would add request IDs or timing markers, use the browser Network and Performance tools, and deliberately introduce latency to expose ordering or rendering behavior that is hidden on a fast local machine.", "More rendering work, synchronous JavaScript or aggressive client caching can trade simplicity for responsiveness or freshness. I protect the UI thread, cancel stale work and measure before optimizing."],["Do not describe react state batching as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-048","L3","React / Browser","Race conditions in data fetching","A fast-changing UI can have multiple requests in flight whose responses arrive out of order.","The latest request should control visible state; abort signals, request IDs or libraries can prevent stale responses from winning.","const c=new AbortController(); fetch(url,{signal:c.signal});","I treat cancellation and stale-result protection as part of correctness, not just optimization.",["What failure does race conditions in data fetching help you diagnose?", "What trade-off should you consider with race conditions in data fetching?"],["I would add request IDs or timing markers, use the browser Network and Performance tools, and deliberately introduce latency to expose ordering or rendering behavior that is hidden on a fast local machine.", "More rendering work, synchronous JavaScript or aggressive client caching can trade simplicity for responsiveness or freshness. I protect the UI thread, cancel stale work and measure before optimizing."],["Do not describe race conditions in data fetching as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-049","L3","Docker","Image layers","Docker images are built from layered filesystem changes that can be reused between builds and images.","Each Dockerfile instruction can contribute a layer; cache reuse depends on instruction inputs and ordering.","COPY package.json ./\nRUN npm ci","I optimize layer boundaries around stable dependencies and frequently changing source.",["What failure does image layers help you diagnose?", "What trade-off should you consider with image layers?"],["I would inspect the image layers and entrypoint, process tree, environment, mounts and network namespace, then compare container behavior with the host-level dependency assumptions.", "Smaller and more restricted images reduce attack surface and startup cost, but overly minimal images can make diagnosis harder. I keep runtime images lean while preserving health, logging and operational visibility."],["Do not describe image layers as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-050","L3","Docker","Container namespaces","Containers isolate processes using Linux kernel namespaces while sharing the host kernel.","PID, network, mount and other namespaces create isolated views; cgroups handle resource control separately.","docker run --pid=private app","I distinguish isolation from virtualization because containers do not include a separate kernel.",["What failure does container namespaces help you diagnose?", "What trade-off should you consider with container namespaces?"],["I would inspect the image layers and entrypoint, process tree, environment, mounts and network namespace, then compare container behavior with the host-level dependency assumptions.", "Smaller and more restricted images reduce attack surface and startup cost, but overly minimal images can make diagnosis harder. I keep runtime images lean while preserving health, logging and operational visibility."],["Do not describe container namespaces as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-051","L3","Docker","Container networking","Container networking gives processes virtual interfaces and connectivity through Docker networks.","Bridge networks provide service connectivity; published ports connect host traffic to container ports.","docker run --network appnet api","I separate container-to-container ports from host-published ports when debugging connectivity.",["What failure does container networking help you diagnose?", "What trade-off should you consider with container networking?"],["I would inspect the image layers and entrypoint, process tree, environment, mounts and network namespace, then compare container behavior with the host-level dependency assumptions.", "Smaller and more restricted images reduce attack surface and startup cost, but overly minimal images can make diagnosis harder. I keep runtime images lean while preserving health, logging and operational visibility."],["Do not describe container networking as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-052","L3","Docker","Multi-stage builds","Multi-stage builds separate build tooling from the final runtime image.","A builder stage compiles the application, and the runtime stage copies only required artifacts.","FROM eclipse-temurin:21 AS build\nFROM eclipse-temurin:21-jre","I use multi-stage builds to reduce attack surface and image size without sacrificing build reproducibility.",["What failure does multi-stage builds help you diagnose?", "What trade-off should you consider with multi-stage builds?"],["I would inspect the image layers and entrypoint, process tree, environment, mounts and network namespace, then compare container behavior with the host-level dependency assumptions.", "Smaller and more restricted images reduce attack surface and startup cost, but overly minimal images can make diagnosis harder. I keep runtime images lean while preserving health, logging and operational visibility."],["Do not describe multi-stage builds as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-053","L3","Docker","Container PID 1","PID 1 inside a container has special signal and child-reaping responsibilities.","If the application is not an appropriate init process, signals may not propagate correctly and zombie processes can accumulate.","docker stop api","I verify graceful shutdown with the actual process tree rather than assuming SIGTERM reaches the app.",["What failure does container pid 1 help you diagnose?", "What trade-off should you consider with container pid 1?"],["I would inspect the image layers and entrypoint, process tree, environment, mounts and network namespace, then compare container behavior with the host-level dependency assumptions.", "Smaller and more restricted images reduce attack surface and startup cost, but overly minimal images can make diagnosis harder. I keep runtime images lean while preserving health, logging and operational visibility."],["Do not describe container pid 1 as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-054","L3","Docker","Docker healthchecks","A healthcheck expresses whether a container is functioning according to an application-level probe.","The runtime executes the check and records health status, which orchestration systems can use as one input for decisions.","HEALTHCHECK CMD curl -f http://localhost:8080/actuator/health","I keep health checks cheap and meaningful; a process being alive is not necessarily being ready.",["What failure does docker healthchecks help you diagnose?", "What trade-off should you consider with docker healthchecks?"],["I would inspect the image layers and entrypoint, process tree, environment, mounts and network namespace, then compare container behavior with the host-level dependency assumptions.", "Smaller and more restricted images reduce attack surface and startup cost, but overly minimal images can make diagnosis harder. I keep runtime images lean while preserving health, logging and operational visibility."],["Do not describe docker healthchecks as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-055","L3","Kubernetes","API server and desired state","The Kubernetes API server is the front door for cluster state and API operations.","Clients write desired state; controllers observe resources and reconcile actual state toward that desired state.","kubectl apply -f deployment.yaml","I explain Kubernetes as an API plus reconciliation system rather than a collection of shell commands.",["What failure does api server and desired state help you diagnose?", "What trade-off should you consider with api server and desired state?"],["I would start with the workload's desired state, Pod events, controller status, Service endpoints and node conditions. Then I would inspect metrics and logs at the first failing boundary.", "Higher availability and faster recovery usually require replicas, capacity headroom and operational complexity. I size requests, replicas, disruption budgets and autoscaling around real workload and dependency limits."],["Do not describe api server and desired state as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-056","L3","Kubernetes","Scheduler decision","The scheduler chooses a node for an unscheduled Pod based on constraints and available resources.","It filters infeasible nodes, scores candidates and binds the Pod to a selected node.","kubectl describe pod api-0","I diagnose Pending Pods by reading scheduler events and constraints before changing random node settings.",["What failure does scheduler decision help you diagnose?", "What trade-off should you consider with scheduler decision?"],["I would start with the workload's desired state, Pod events, controller status, Service endpoints and node conditions. Then I would inspect metrics and logs at the first failing boundary.", "Higher availability and faster recovery usually require replicas, capacity headroom and operational complexity. I size requests, replicas, disruption budgets and autoscaling around real workload and dependency limits."],["Do not describe scheduler decision as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-057","L3","Kubernetes","Controller reconciliation","Controllers continuously compare desired and observed state and take actions to converge them.","A Deployment controller creates or updates ReplicaSets and Pods so the observed state approaches the declared replica and template state.","spec.replicas: 3","I describe reconciliation as an ongoing loop, which explains why deleting a managed Pod often causes it to return.",["What failure does controller reconciliation help you diagnose?", "What trade-off should you consider with controller reconciliation?"],["I would start with the workload's desired state, Pod events, controller status, Service endpoints and node conditions. Then I would inspect metrics and logs at the first failing boundary.", "Higher availability and faster recovery usually require replicas, capacity headroom and operational complexity. I size requests, replicas, disruption budgets and autoscaling around real workload and dependency limits."],["Do not describe controller reconciliation as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-058","L3","Kubernetes","Service endpoint selection","A Kubernetes Service provides a stable virtual destination for a changing set of Pods.","Selectors identify eligible Pods and endpoint data is updated as Pods become ready or unready.","selector:\n  app: api","I debug Service failures by checking selectors, endpoints and readiness before blaming networking.",["What failure does service endpoint selection help you diagnose?", "What trade-off should you consider with service endpoint selection?"],["I would start with the workload's desired state, Pod events, controller status, Service endpoints and node conditions. Then I would inspect metrics and logs at the first failing boundary.", "Higher availability and faster recovery usually require replicas, capacity headroom and operational complexity. I size requests, replicas, disruption budgets and autoscaling around real workload and dependency limits."],["Do not describe service endpoint selection as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-059","L3","Kubernetes","Probe semantics","Startup, readiness and liveness probes answer different questions about an application.","Startup protects slow initialization, readiness controls traffic eligibility, and liveness detects a stuck process that should be restarted.","readinessProbe:\n  httpGet: {path: /ready, port: 8080}","I never use liveness to represent dependency health when doing so could create restart storms.",["What failure does probe semantics help you diagnose?", "What trade-off should you consider with probe semantics?"],["I would start with the workload's desired state, Pod events, controller status, Service endpoints and node conditions. Then I would inspect metrics and logs at the first failing boundary.", "Higher availability and faster recovery usually require replicas, capacity headroom and operational complexity. I size requests, replicas, disruption budgets and autoscaling around real workload and dependency limits."],["Do not describe probe semantics as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-060","L3","Kubernetes","HPA control loop","Horizontal Pod Autoscaler changes desired replica count from observed metrics.","The controller compares current metric values with targets and adjusts replicas subject to min/max and timing constraints.","kubectl get hpa","I explain HPA as a feedback controller and include metric delay and downstream capacity in the design.",["What failure does hpa control loop help you diagnose?", "What trade-off should you consider with hpa control loop?"],["I would start with the workload's desired state, Pod events, controller status, Service endpoints and node conditions. Then I would inspect metrics and logs at the first failing boundary.", "Higher availability and faster recovery usually require replicas, capacity headroom and operational complexity. I size requests, replicas, disruption budgets and autoscaling around real workload and dependency limits."],["Do not describe hpa control loop as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-061","L3","CI/CD / GitHub Actions","Workflow execution graph","A CI workflow is a graph of jobs and dependencies, not just a script.","Triggers create runs; jobs execute on runners, and needs/outputs determine ordering and data flow.","jobs: { test: { runs-on: ubuntu-latest } }","I design the graph around independent work, artifact boundaries and release gates.",["What failure does workflow execution graph help you diagnose?", "What trade-off should you consider with workflow execution graph?"],["I would verify the dependency graph, artifact identity, permissions, environment gates and deployment verification. For cloud access I would inspect the OIDC claims and resulting short-lived role rather than stored secrets.", "More gates and verification reduce release risk but increase delivery time. I keep fast checks early, promote immutable artifacts, and reserve expensive or human-controlled gates for production risk."],["Do not describe workflow execution graph as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-062","L3","CI/CD / GitHub Actions","Artifact immutability","An immutable artifact is built once and promoted through environments without rebuilding.","The pipeline records a version or digest and later environments deploy that exact artifact.","image: app@sha256:...","I prefer immutable promotion because rebuilding between environments can change the thing being tested.",["What failure does artifact immutability help you diagnose?", "What trade-off should you consider with artifact immutability?"],["I would verify the dependency graph, artifact identity, permissions, environment gates and deployment verification. For cloud access I would inspect the OIDC claims and resulting short-lived role rather than stored secrets.", "More gates and verification reduce release risk but increase delivery time. I keep fast checks early, promote immutable artifacts, and reserve expensive or human-controlled gates for production risk."],["Do not describe artifact immutability as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-063","L3","CI/CD / GitHub Actions","OIDC workload identity","OIDC lets CI exchange a short-lived identity token for cloud credentials without storing long-lived keys.","The cloud provider validates claims such as repository, branch or environment before issuing temporary credentials.","permissions:\n  id-token: write","I constrain trust conditions tightly and grant the resulting role only the deployment permissions required.",["What failure does oidc workload identity help you diagnose?", "What trade-off should you consider with oidc workload identity?"],["I would verify the dependency graph, artifact identity, permissions, environment gates and deployment verification. For cloud access I would inspect the OIDC claims and resulting short-lived role rather than stored secrets.", "More gates and verification reduce release risk but increase delivery time. I keep fast checks early, promote immutable artifacts, and reserve expensive or human-controlled gates for production risk."],["Do not describe oidc workload identity as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-064","L3","CI/CD / GitHub Actions","Concurrency control","CI concurrency prevents overlapping runs from racing over shared deployment targets.","Workflow groups can cancel obsolete runs or serialize operations so only one relevant deployment proceeds.","concurrency:\n  group: production","I use concurrency for deployment safety, while allowing independent validation work to run in parallel.",["What failure does concurrency control help you diagnose?", "What trade-off should you consider with concurrency control?"],["I would verify the dependency graph, artifact identity, permissions, environment gates and deployment verification. For cloud access I would inspect the OIDC claims and resulting short-lived role rather than stored secrets.", "More gates and verification reduce release risk but increase delivery time. I keep fast checks early, promote immutable artifacts, and reserve expensive or human-controlled gates for production risk."],["Do not describe concurrency control as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-065","L3","CI/CD / GitHub Actions","Cache correctness","A CI cache speeds builds by reusing derived data, but a stale or overly broad cache can corrupt reproducibility.","Cache keys should include inputs that materially affect the derived dependency state, with restore keys used carefully.","key: maven-${{ hashFiles('**/pom.xml') }}","I treat caches as performance hints, never as authoritative build inputs.",["What failure does cache correctness help you diagnose?", "What trade-off should you consider with cache correctness?"],["I would verify the dependency graph, artifact identity, permissions, environment gates and deployment verification. For cloud access I would inspect the OIDC claims and resulting short-lived role rather than stored secrets.", "More gates and verification reduce release risk but increase delivery time. I keep fast checks early, promote immutable artifacts, and reserve expensive or human-controlled gates for production risk."],["Do not describe cache correctness as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-066","L3","CI/CD / GitHub Actions","Deployment verification","A deployment is incomplete until the pipeline proves the new version is healthy.","Verification can combine rollout status, health endpoints, metrics and smoke tests before promotion or completion.","kubectl rollout status deployment/api","I make verification an explicit stage so a successful command is not mistaken for a successful release.",["What failure does deployment verification help you diagnose?", "What trade-off should you consider with deployment verification?"],["I would verify the dependency graph, artifact identity, permissions, environment gates and deployment verification. For cloud access I would inspect the OIDC claims and resulting short-lived role rather than stored secrets.", "More gates and verification reduce release risk but increase delivery time. I keep fast checks early, promote immutable artifacts, and reserve expensive or human-controlled gates for production risk."],["Do not describe deployment verification as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-067","L3","AWS / Cloud","VPC routing","A VPC routes packets between subnets and connected networks according to route tables.","Each subnet is associated with a route table; local routes handle VPC traffic while gateways handle external destinations.","0.0.0.0/0 -> NAT Gateway","I debug cloud connectivity by tracing source subnet, route table, gateway and security controls in order.",["What failure does vpc routing help you diagnose?", "What trade-off should you consider with vpc routing?"],["I would trace source, subnet, route table, gateway/load balancer, security policy, target health and application response in that order. For IAM I would identify principal, action, resource and explicit denies.", "Scaling application instances without considering database connections, network limits or downstream quotas can move the bottleneck rather than remove it. I scale the constrained resource and verify the new limiting factor."],["Do not describe vpc routing as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-068","L3","AWS / Cloud","Security groups","Security groups act as stateful virtual firewalls attached to supported resources.","Inbound and outbound rules control allowed traffic, and return traffic is tracked as part of the connection state.","TCP 443 from ALB-SG","I prefer referencing security groups or narrow CIDRs instead of opening broad network access.",["What failure does security groups help you diagnose?", "What trade-off should you consider with security groups?"],["I would trace source, subnet, route table, gateway/load balancer, security policy, target health and application response in that order. For IAM I would identify principal, action, resource and explicit denies.", "Scaling application instances without considering database connections, network limits or downstream quotas can move the bottleneck rather than remove it. I scale the constrained resource and verify the new limiting factor."],["Do not describe security groups as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-069","L3","AWS / Cloud","IAM policy evaluation","IAM determines whether an action is allowed from applicable policies and explicit denies.","Identity/resource policies, conditions, permissions boundaries and other controls combine to determine the effective permission.","Action: s3:GetObject","I debug AccessDenied by identifying the principal, action, resource and every policy layer affecting the decision.",["What failure does iam policy evaluation help you diagnose?", "What trade-off should you consider with iam policy evaluation?"],["I would trace source, subnet, route table, gateway/load balancer, security policy, target health and application response in that order. For IAM I would identify principal, action, resource and explicit denies.", "Scaling application instances without considering database connections, network limits or downstream quotas can move the bottleneck rather than remove it. I scale the constrained resource and verify the new limiting factor."],["Do not describe iam policy evaluation as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-070","L3","AWS / Cloud","Load balancer request flow","A load balancer accepts client traffic and forwards it to healthy targets.","Listeners apply protocol/port rules, target groups determine backend membership and health checks control eligibility.","client -> ALB:443 -> target:8080","I troubleshoot 502 by checking listener, target health, network path and application response in that order.",["What failure does load balancer request flow help you diagnose?", "What trade-off should you consider with load balancer request flow?"],["I would trace source, subnet, route table, gateway/load balancer, security policy, target health and application response in that order. For IAM I would identify principal, action, resource and explicit denies.", "Scaling application instances without considering database connections, network limits or downstream quotas can move the bottleneck rather than remove it. I scale the constrained resource and verify the new limiting factor."],["Do not describe load balancer request flow as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-071","L3","AWS / Cloud","RDS connection behavior","Managed databases still have finite connection, CPU, memory and I/O capacity.","Applications use pooled connections; excessive concurrency can exhaust the database even when application replicas appear healthy.","maximumPoolSize=20","I size application pools from database capacity and workload rather than per-Pod CPU alone.",["What failure does rds connection behavior help you diagnose?", "What trade-off should you consider with rds connection behavior?"],["I would trace source, subnet, route table, gateway/load balancer, security policy, target health and application response in that order. For IAM I would identify principal, action, resource and explicit denies.", "Scaling application instances without considering database connections, network limits or downstream quotas can move the bottleneck rather than remove it. I scale the constrained resource and verify the new limiting factor."],["Do not describe rds connection behavior as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error."),
lesson("iw-072","L3","AWS / Cloud","Cloud observability","Cloud observability combines infrastructure and application signals to explain system behavior.","Metrics reveal resource trends, logs provide event detail, and traces connect distributed request latency across services.","traceId=abc123","I correlate signals around one request or incident rather than reading dashboards in isolation.",["What failure does cloud observability help you diagnose?", "What trade-off should you consider with cloud observability?"],["I would trace source, subnet, route table, gateway/load balancer, security policy, target health and application response in that order. For IAM I would identify principal, action, resource and explicit denies.", "Scaling application instances without considering database connections, network limits or downstream quotas can move the bottleneck rather than remove it. I scale the constrained resource and verify the new limiting factor."],["Do not describe cloud observability as magic; name the runtime boundary that actually performs the work.", "Do not optimize or change configuration before establishing the failure with evidence."],"In production, this concept matters when the system crosses an abstraction boundary. Understanding the internal mechanism lets you predict behavior, interpret telemetry and choose a targeted fix instead of relying on trial and error.")
];
