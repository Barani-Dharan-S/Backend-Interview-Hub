const baseFullStackLessons = [
{id:'fullstack-001',level:'L1',title:'How React and Spring Boot fit together',subtopic:'Architecture',what:'A typical full-stack application uses React for the browser UI and Spring Boot for HTTP APIs, business logic, security, and persistence.',how:'React renders UI and sends HTTP requests. Spring Boot receives requests through controllers, executes service logic, accesses persistence, and returns JSON. The browser then updates its state from the response.',code:`React UI\n  -> HTTP/JSON\n  -> Spring Boot Controller\n  -> Service\n  -> Repository\n  -> Database\n  -> JSON response\n  -> React state`,delivery:'I explain the boundary clearly: React owns presentation and client interaction, while Spring Boot owns server-side business rules, authorization, and data access. The API contract connects them.',followups:['Where should validation happen?','Where should authorization happen?','How would you debug a failing request?'],traps:['Do not treat hiding a React button as backend authorization.']},
{id:'fullstack-002',level:'L1',title:'Designing a REST API for a React application',subtopic:'REST APIs',what:'A frontend-friendly REST API exposes stable resources and predictable HTTP semantics so React can fetch, create, update, and delete data.',how:'Define resource URLs, methods, request/response DTOs, validation rules, status codes, pagination, and error contracts before coupling the UI to implementation details.',code:`GET    /api/orders?page=0&size=20\nPOST   /api/orders\nGET    /api/orders/{id}\nPATCH  /api/orders/{id}\nDELETE /api/orders/{id}`,delivery:'I design the API contract first because React should depend on stable DTOs and semantics, not Spring entity internals.',followups:['Why use DTOs?','PUT vs PATCH?','How do you version an API?'],traps:['Do not expose JPA entities directly when the API contract needs independent evolution.']},
{id:'fullstack-003',level:'L1',title:'DTOs between React and Spring Boot',subtopic:'REST APIs',what:'DTOs are explicit request and response shapes used at the API boundary instead of exposing persistence models directly.',how:'The controller accepts request DTOs, validates them, maps them to domain/service inputs, and maps results to response DTOs.',code:`public record CreateOrderRequest(String productId, int quantity) {}\npublic record OrderResponse(Long id, String status) {}`,delivery:'I use DTOs to keep the HTTP contract stable and prevent persistence concerns from leaking into the frontend.',followups:['Entity vs DTO?','Where should mapping happen?'],traps:['Do not assume DTOs automatically validate business rules.']},
{id:'fullstack-004',level:'L1',title:'Handling loading, success, empty and error states',subtopic:'Data Fetching',what:'A production UI needs more than a single loading boolean; it should represent meaningful request states.',how:'React state can distinguish initial loading, successful data, empty results, recoverable errors, and retrying. The API should provide useful status codes and error payloads.',code:`if (loading) return <Spinner />;\nif (error) return <Retry message={error.message} />;\nif (!items.length) return <EmptyState />;\nreturn <OrderList items={items} />;`,delivery:'I model the complete UI state rather than only the happy path. This makes API failures understandable to users and easier to debug.',followups:['How do you retry?','What belongs in the API error body?'],traps:['Do not show raw server stack traces to users.']},
{id:'fullstack-005',level:'L1',title:'CORS between React and Spring Boot',subtopic:'Security',what:'CORS controls which browser origins may make cross-origin requests to a server.',how:'The browser enforces the policy. Spring Boot can return the required Access-Control-Allow-* headers for approved origins, methods, and headers. Some requests trigger a preflight OPTIONS request.',code:`@Bean\nCorsConfigurationSource corsConfigurationSource() {\n  CorsConfiguration c = new CorsConfiguration();\n  c.setAllowedOrigins(List.of("https://app.example.com"));\n  c.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE"));\n  return request -> c;\n}`,delivery:'CORS is a browser-origin policy, not an authentication mechanism. I configure explicit production origins and understand preflight behavior.',followups:['Why does preflight happen?','CORS vs CSRF?','Why does Postman not show the same CORS error?'],traps:['Do not use allow-all origins with credentials casually.']},
{id:'fullstack-006',level:'L2',title:'JWT authentication from React to Spring Security',subtopic:'Authentication',what:'React can authenticate through a backend endpoint and use the resulting credential for protected API calls.',how:'The backend authenticates credentials and issues a token according to the chosen architecture. Spring Security validates the token on protected requests and establishes the authenticated principal.',code:`fetch('/api/orders', {\n  headers: { Authorization: \`Bearer \${token}\` }\n});`,delivery:'The backend remains the authority for authentication and authorization. React uses the credential to make requests but must never be trusted to decide whether a user is allowed to perform an operation.',followups:['Where should tokens be stored?','How does refresh work?','What happens after a 401?'],traps:['Never put authorization decisions only in React.']},
{id:'fullstack-007',level:'L2',title:'Protected React routes and backend authorization',subtopic:'Security',what:'Protected routes improve user experience by preventing navigation to pages that require authentication, while backend authorization enforces the real security boundary.',how:'React checks available authentication state before rendering protected UI. Spring Security independently validates the request and required authorities before allowing the operation.',code:`<Route path="/admin" element={\n  <RequireAuth role="ADMIN"><AdminPage /></RequireAuth>\n} />`,delivery:'I use frontend route guards for UX and backend authorization for security. Both layers have different responsibilities.',followups:['RBAC vs ABAC?','How should a 403 be handled?'],traps:['A hidden route is not a security control.']},
{id:'fullstack-008',level:'L2',title:'401 vs 403 in a full-stack application',subtopic:'Security',what:'401 indicates that authentication is missing or invalid; 403 indicates that the request is understood but the authenticated principal is not allowed to perform it.',how:'Spring Security produces the appropriate response. React can redirect or refresh authentication for 401 and show an authorization message for 403.',code:`401 -> authenticate / refresh session\n403 -> authenticated but forbidden`,delivery:'I distinguish authentication failure from authorization failure because the frontend response should be different and the backend contract should remain explicit.',followups:['Where is the status generated?','Should React retry a 403?'],traps:['Do not automatically retry authorization failures.']},
{id:'fullstack-009',level:'L2',title:'Global error handling with Spring Boot and React',subtopic:'Error Handling',what:'A consistent backend error contract lets the frontend display useful messages without parsing arbitrary exception text.',how:'Spring Boot can use a global exception handler to map validation, business, authentication, authorization, and unexpected errors into structured responses. React translates those responses into user-facing states.',code:`{\n  "code": "ORDER_NOT_FOUND",\n  "message": "Order was not found",\n  "traceId": "abc-123"\n}`,delivery:'I define stable machine-readable error codes and keep internal stack traces out of the browser. React handles known errors explicitly and falls back safely for unknown ones.',followups:['Where should validation errors go?','Why include a trace ID?'],traps:['Do not expose exception class names or stack traces to clients.']},
{id:'fullstack-010',level:'L2',title:'Request validation in React and Spring Boot',subtopic:'Validation',what:'Frontend validation improves usability, while backend validation is mandatory because clients are untrusted.',how:'React can validate required fields and formats before submission. Spring Boot validates request DTOs and applies business rules before changing state.',code:`public record CreateUserRequest(\n  @NotBlank String email,\n  @Size(min = 8) String password\n) {}`,delivery:'I validate on both sides for different reasons: React gives immediate feedback, while Spring Boot enforces the authoritative contract and business rules.',followups:['Bean Validation vs business validation?','How do you return field errors?'],traps:['Never rely on React validation for security.']},
{id:'fullstack-011',level:'L2',title:'Pagination from database to React',subtopic:'Performance',what:'Pagination limits the amount of data transferred, processed, and rendered at one time.',how:'Spring Boot accepts page and size parameters, applies database pagination, and returns content plus metadata. React renders the current page and controls navigation.',code:`GET /api/orders?page=0&size=20\n\n{ "content": [...], "page": 0, "size": 20, "totalElements": 137 }`,delivery:'I paginate at the database and API layers instead of fetching everything and slicing it in React. This keeps network, memory, and rendering costs bounded.',followups:['Offset vs cursor pagination?','How should sorting be validated?'],traps:['Do not trust arbitrary sort field names from the client.']},
{id:'fullstack-012',level:'L2',title:'Search and filtering across React and Spring Boot',subtopic:'Performance',what:'Search should usually be executed where the authoritative dataset lives rather than filtering a partial client cache.',how:'React debounces user input and sends query parameters. Spring Boot validates filters and delegates efficient predicates to the database.',code:`GET /api/orders?status=SHIPPED&query=truck&page=0&size=20`,delivery:'For large datasets I keep filtering server-side. React manages input state and request timing, while the backend owns query semantics and database efficiency.',followups:['How do you debounce?','What indexes would you add?'],traps:['Do not assume client-side filtering scales with dataset size.']},
{id:'fullstack-013',level:'L2',title:'File upload from React to Spring Boot',subtopic:'Files',what:'A file upload sends binary content and associated metadata to the backend for validation and storage.',how:'React uses FormData and multipart/form-data. Spring Boot accepts MultipartFile, validates type and size, then stores it in object storage or another controlled location.',code:`const form = new FormData();\nform.append('file', file);\nawait fetch('/api/documents', { method: 'POST', body: form });`,delivery:'I validate file size, type, authorization and naming on the backend. The browser experience is separate from the storage and security decision.',followups:['Where should files be stored?','How do you scan uploads?'],traps:['Do not trust the browser-provided MIME type as the only validation.']},
{id:'fullstack-014',level:'L2',title:'Transactions and user experience',subtopic:'Consistency',what:'A backend transaction protects a database operation boundary; it does not automatically make multiple browser requests atomic.',how:'Spring Boot groups related database writes in a transaction. React should treat the API response as the authoritative result and handle partial UI workflows explicitly.',code:`@Transactional\npublic Order createOrder(CreateOrderRequest request) {\n  // validate, persist order, persist items\n}`,delivery:'I keep transaction boundaries on the backend where the database transaction exists. The frontend coordinates user interaction but cannot provide database atomicity.',followups:['What happens on rollback?','How do you handle long-running workflows?'],traps:['Do not claim that a React state update participates in a Spring transaction.']},
{id:'fullstack-015',level:'L2',title:'Optimistic updates with a Spring Boot API',subtopic:'UX Patterns',what:'An optimistic React update shows the expected result before the server confirms it, then reconciles or rolls back.',how:'The UI stores enough previous state to undo the change, sends the API request, and reconciles with the server response. The backend remains authoritative.',code:`setLiked(true);\ntry {\n  await api.post(\`/api/posts/\${id}/like\`);\n} catch {\n  setLiked(false);\n}`,delivery:'I use optimistic updates only when rollback and concurrency behavior are well defined. For critical operations I prefer confirmed server state.',followups:['What if two users update the same record?','How do idempotency keys help?'],traps:['Never assume an optimistic UI change succeeded permanently.']},
{id:'fullstack-016',level:'L2',title:'API client layer in React',subtopic:'Architecture',what:'A centralized API client keeps HTTP concerns out of individual components.',how:'Components call domain-oriented functions or hooks. The client handles base URLs, serialization, common headers, error normalization, and request instrumentation.',code:`export async function getOrders(params) {\n  const response = await fetch(\`\${API_BASE}/orders?\${params}\`);\n  if (!response.ok) throw await toApiError(response);\n  return response.json();\n}`,delivery:'I prefer a thin API layer so components express UI intent instead of repeating fetch, header, parsing, and error logic everywhere.',followups:['Where should auth headers be added?','How would you test the client?'],traps:['Do not hide all domain behavior inside one giant API utility.']},
{id:'fullstack-017',level:'L2',title:'Environment configuration across frontend and backend',subtopic:'Deployment',what:'Frontend and backend have different configuration boundaries because frontend configuration is shipped to the browser.',how:'React receives public configuration such as an API base URL at build or runtime. Spring Boot can access server-side secrets through environment or secret management systems.',code:`React: VITE_API_BASE_URL=https://api.example.com\nSpring: DB_PASSWORD=<secret-manager-value>`,delivery:'I treat browser configuration as public and keep secrets on the server. Environment strategy should be explicit for local, test, staging, and production.',followups:['Build-time vs runtime frontend config?','Where do database credentials live?'],traps:['Never put private backend credentials into a browser bundle.']},
{id:'fullstack-018',level:'L2',title:'API versioning and frontend compatibility',subtopic:'API Evolution',what:'Independent frontend and backend deployments require an API evolution strategy so old clients do not break unexpectedly.',how:'Prefer backward-compatible changes where possible, deprecate fields gradually, and introduce explicit versions only when compatibility cannot be maintained otherwise.',code:`/api/v1/orders\n/api/v2/orders`,delivery:'I design API changes for coexistence. Adding optional response fields is usually safer than changing the meaning or type of an existing field.',followups:['When do you version?','How do you deprecate an endpoint?'],traps:['Do not version every minor change without a compatibility reason.']},
{id:'fullstack-019',level:'L2',title:'Idempotency for React retries',subtopic:'Reliability',what:'Retries can accidentally create duplicate server-side effects unless the API supports idempotency.',how:'React or an API client may retry after timeouts. The backend accepts an idempotency key, records the result for the operation, and returns the same outcome for duplicate attempts.',code:`POST /api/payments\nIdempotency-Key: 7f3e...`,delivery:'For operations such as payments or order creation, I design the backend to tolerate client retries because a network timeout does not tell the client whether the server completed the operation.',followups:['Where is the key stored?','How long should it be valid?'],traps:['Do not equate HTTP POST with automatically safe retries.']},
{id:'fullstack-020',level:'L2',title:'Correlation IDs across React and Spring Boot',subtopic:'Observability',what:'A correlation or trace ID connects browser activity with backend logs and downstream operations.',how:'The frontend sends or receives a trace identifier according to the tracing strategy. Spring Boot propagates tracing context through service calls and logs it with errors and timings.',code:`X-Correlation-Id: 9f2a1c\n\nBrowser -> API -> Service -> DB`,delivery:'When debugging a production issue, I want one identifier that lets me move from the browser network entry to backend logs and downstream traces.',followups:['Where should the ID be generated?','How does distributed tracing differ?'],traps:['Do not log tokens or sensitive payloads just to improve correlation.']},
{id:'fullstack-021',level:'L3',title:'Designing a full-stack feature end-to-end',subtopic:'Architecture',what:'A production feature should be designed as a coherent flow across UI, API, domain logic, persistence, security, testing and observability.',how:'Start with the user journey and API contract, then define React state and components, Spring DTO/controller/service/repository boundaries, data model, authorization, failure states, tests and deployment behavior.',code:`Feature\n ├─ React page + components\n ├─ API client + hook\n ├─ REST DTOs\n ├─ Service + domain rules\n ├─ Repository + DB\n ├─ Security\n ├─ Tests\n └─ Metrics/logs/traces`,delivery:'I design the vertical slice first. That exposes cross-layer dependencies early and gives the team a testable path from user action to persisted outcome.',followups:['Where would you start?','How do you split frontend and backend ownership?'],traps:['Do not design only the UI and postpone API semantics until the end.']},
{id:'fullstack-022',level:'L3',title:'BFF vs direct React-to-microservice calls',subtopic:'Architecture',what:'A Backend-for-Frontend can provide a client-specific API facade instead of exposing many microservices directly to the browser.',how:'The browser calls one BFF. The BFF handles aggregation, authentication context, client-specific shaping and calls internal services. Direct calls can be simpler for small systems but increase coupling to internal APIs.',code:`React -> BFF -> Order Service\n             -> Customer Service\n             -> Inventory Service`,delivery:'I consider a BFF when the frontend needs aggregation or a stable client-facing contract across many backend services. I avoid it when it adds a proxy layer without solving a real problem.',followups:['BFF vs API gateway?','Who owns authentication?'],traps:['Do not assume every microservice architecture needs a BFF.']},
{id:'fullstack-023',level:'L3',title:'API Gateway in a React microservices application',subtopic:'Architecture',what:'An API gateway can provide a single external entry point for routing, cross-cutting policies and service discovery abstraction.',how:'React calls the gateway, which authenticates or forwards identity, routes to services, and may apply rate limits, observability and request policies.',code:`React\n  -> API Gateway\n      -> Order Service\n      -> User Service\n      -> Catalog Service`,delivery:'The gateway simplifies the public boundary, but business authorization and domain rules still belong in the appropriate services.',followups:['Gateway vs load balancer?','Where should rate limiting live?'],traps:['Do not put all business logic into the gateway.']},
{id:'fullstack-024',level:'L3',title:'Handling concurrent edits',subtopic:'Consistency',what:'Two users can read the same version of a resource and then attempt conflicting updates.',how:'Use optimistic locking such as a version field or ETag. The backend detects stale updates and returns a conflict response. React can refresh and ask the user to reconcile.',code:`GET /api/orders/42\nETag: "v7"\n\nPATCH /api/orders/42\nIf-Match: "v7"`,delivery:'For collaborative or frequently edited data, I prefer explicit concurrency control rather than silently overwriting another user\'s changes.',followups:['What status code would you return?','How does @Version work?'],traps:['Do not solve concurrency by blindly retrying a stale write.']},
{id:'fullstack-025',level:'L3',title:'Caching in a React + Spring Boot application',subtopic:'Performance',what:'Caching can reduce repeated work and latency, but cache ownership and invalidation must be explicit.',how:'Browser or frontend caches can avoid repeated network calls. Backend caches can avoid expensive database or downstream calls. The backend must define freshness and invalidation semantics for authoritative data.',code:`React cache -> HTTP API -> Spring cache -> Database`,delivery:'I first identify what is safe to cache and the required freshness. Then I choose client, HTTP, backend or distributed caching based on ownership and invalidation needs.',followups:['What should not be cached?','Cache-aside vs write-through?'],traps:['Do not cache sensitive user-specific data without considering isolation and invalidation.']},
{id:'fullstack-026',level:'L3',title:'ETags and conditional requests',subtopic:'Performance',what:'ETags let a client ask whether a resource changed before downloading it again.',how:'Spring Boot returns an ETag with a representation. React or the browser sends If-None-Match. If unchanged, the server can return 304 Not Modified.',code:`GET /api/profile\nIf-None-Match: "abc"\n\nHTTP/1.1 304 Not Modified`,delivery:'Conditional requests reduce bandwidth for resources that change infrequently. They are an HTTP caching mechanism, not a replacement for application state management.',followups:['ETag vs Last-Modified?','Can ETags be used for concurrency?'],traps:['Do not treat a 304 as a JSON response body.']},
{id:'fullstack-027',level:'L3',title:'Timeouts, retries and backoff across the stack',subtopic:'Reliability',what:'A full-stack system needs bounded timeouts and carefully scoped retries because downstream slowness can otherwise cascade.',how:'The frontend may retry selected transient requests. Spring Boot clients use connection/read timeouts and exponential backoff where appropriate. Non-idempotent operations require special care.',code:`attempt 1 -> 200ms\nattempt 2 -> 400ms\nattempt 3 -> 800ms\nwith jitter and a maximum`,delivery:'I set finite timeouts first, then add retries only for transient and retry-safe failures. I avoid retry storms and make idempotency explicit for side effects.',followups:['Where should retry happen?','Why add jitter?'],traps:['Do not retry every 500 response automatically.']},
{id:'fullstack-028',level:'L3',title:'Rate limiting a public full-stack API',subtopic:'Security',what:'Rate limiting protects APIs from abuse and prevents one client from consuming disproportionate capacity.',how:'A gateway or backend can apply limits using identity, API key, IP, route or other dimensions. React should interpret 429 responses and provide an appropriate user experience.',code:`HTTP/1.1 429 Too Many Requests\nRetry-After: 30`,delivery:'I enforce rate limits server-side because the browser cannot be trusted. The frontend can respect Retry-After and avoid hammering the endpoint.',followups:['Where should limits live?','How do distributed rate limiters work?'],traps:['Do not rely on client-side throttling as the security control.']},
{id:'fullstack-029',level:'L3',title:'Secure cookie vs browser token architecture',subtopic:'Security',what:'Authentication credentials can be handled through different browser architectures, each with trade-offs around XSS, CSRF, refresh and operational complexity.',how:'HttpOnly cookies reduce direct JavaScript access to the credential but require CSRF considerations for cookie-authenticated requests. Token-in-memory approaches change persistence and refresh behavior. The correct choice depends on the threat model.',code:`HttpOnly + Secure + SameSite cookie\n        OR\nshort-lived access credential + controlled refresh flow`,delivery:'I choose the credential architecture from the threat model rather than treating localStorage versus cookies as a universal rule. I explicitly address XSS, CSRF, rotation and logout.',followups:['Why HttpOnly?','How does SameSite help?','Where does CSRF protection fit?'],traps:['Do not claim that cookies automatically prevent XSS or CSRF.']},
{id:'fullstack-030',level:'L3',title:'Database transactions with external side effects',subtopic:'Distributed Systems',what:'A database transaction cannot automatically roll back an email, payment provider call, or message sent to an external system.',how:'Use patterns such as an outbox, transactional event publication, idempotent consumers, or workflow orchestration depending on the requirement.',code:`DB transaction\n  -> persist order + outbox event\ncommit\n  -> publisher sends event\n  -> external consumer processes it`,delivery:'I avoid pretending a local database transaction provides distributed atomicity. For reliable side effects I use an outbox or an explicit workflow with idempotency.',followups:['How does the outbox work?','What if publishing fails?'],traps:['Do not call a payment provider inside a transaction and assume rollback can undo it.']},
{id:'fullstack-031',level:'L3',title:'Event-driven React + Spring Boot workflow',subtopic:'Messaging',what:'Some user actions start asynchronous backend workflows instead of completing synchronously in one HTTP request.',how:'React submits a command, Spring Boot persists the request and publishes an event. Consumers process it, and React later reads status or receives updates through polling, SSE or WebSocket.',code:`React -> POST /api/reports\nSpring -> Kafka -> Report Worker\nReact -> GET /api/reports/{id}/status`,delivery:'For long-running work I return a job identifier instead of holding the HTTP request open. The UI displays progress or status while backend consumers do the work asynchronously.',followups:['Polling vs WebSocket?','How do you make processing idempotent?'],traps:['Do not keep a browser request open for minutes just because work is asynchronous.']},
{id:'fullstack-032',level:'L3',title:'WebSocket vs SSE for full-stack applications',subtopic:'Real-Time',what:'WebSocket provides bidirectional communication; Server-Sent Events provide a server-to-browser stream over HTTP.',how:'Choose based on interaction needs. Notifications and server-side progress often fit SSE, while collaborative or interactive bidirectional features may need WebSockets.',code:`SSE:\nReact <- EventSource <- Spring Boot\n\nWebSocket:\nReact <-> WebSocket <-> Spring`,delivery:'I choose the simplest protocol that matches the interaction. SSE is attractive for server-to-client updates; WebSocket is appropriate when the browser also needs low-latency messages back.',followups:['How do you reconnect?','How do you authenticate?'],traps:['Do not choose WebSocket merely because the application is real-time.']},
{id:'fullstack-033',level:'L3',title:'Testing the full-stack boundary',subtopic:'Testing',what:'A full-stack feature needs tests at multiple boundaries instead of relying only on browser end-to-end tests.',how:'Unit-test React logic and Spring services, integration-test API contracts and persistence, and use a smaller set of end-to-end tests for critical user journeys.',code:`React component test\n      +\nSpring Boot integration test\n      +\nAPI contract test\n      +\ncritical E2E flow`,delivery:'I use the test pyramid: fast unit tests for local behavior, integration tests for real boundaries, and a focused E2E suite for high-value journeys.',followups:['What should E2E cover?','How do you mock external services?'],traps:['Do not make every test an expensive browser test.']},
{id:'fullstack-034',level:'L3',title:'Contract testing between React and Spring Boot',subtopic:'Testing',what:'Contract tests verify that an API provider and consumer agree on request and response behavior.',how:'The contract describes paths, parameters, status codes and schemas. Backend changes can be checked against consumer expectations before deployment.',code:`Consumer expects:\nGET /api/orders/42 -> 200\n{ id: number, status: string }`,delivery:'Contract testing reduces integration surprises when frontend and backend are deployed independently. I focus contracts on observable behavior, not internal implementation.',followups:['Consumer-driven contracts?','OpenAPI-based validation?'],traps:['Do not use contract tests to duplicate every backend unit test.']},
{id:'fullstack-035',level:'L3',title:'OpenAPI as a shared full-stack contract',subtopic:'API Design',what:'OpenAPI can describe REST endpoints, request schemas, responses and security requirements in a machine-readable form.',how:'Spring Boot can publish or generate an OpenAPI description. Frontend tooling can use it for documentation or typed client generation, reducing manual drift.',code:`GET /api/orders/{id}\nresponses:\n  200: OrderResponse\n  404: ApiError`,delivery:'I like OpenAPI as a shared contract because it gives frontend and backend teams one explicit description of the HTTP boundary.',followups:['Code generation trade-offs?','How do you keep docs current?'],traps:['Do not assume generated clients remove the need for API design.']},
{id:'fullstack-036',level:'L3',title:'Tracing a 500 error from React to PostgreSQL',subtopic:'Debugging',what:'A full-stack 500 error should be traced across the request path rather than debugged by guessing which layer is responsible.',how:'Start with the browser Network panel: URL, method, payload, status and response. Then use correlation/trace IDs to inspect Spring logs, service exceptions, SQL timing and database constraints.',code:`Browser Network\n -> API log\n -> Controller\n -> Service\n -> SQL\n -> DB constraint/error`,delivery:'I isolate the failing boundary first. A 500 tells me the server failed, but logs and traces tell me whether the cause is validation, business logic, a dependency or persistence.',followups:['What if the browser shows a CORS error?','How do you distinguish 4xx from 5xx?'],traps:['Do not start changing React state before confirming the HTTP response.']},
{id:'fullstack-037',level:'L3',title:'Diagnosing slow page load',subtopic:'Performance',what:'Page performance can be limited by browser work, network latency, API latency, database queries, or payload size.',how:'Measure separately: browser rendering and bundle size, network waterfall, backend timing, downstream calls, database query plans, and response payloads.',code:`Page load\n -> JS bundle\n -> API latency\n -> DB query\n -> response size\n -> render/interaction`,delivery:'I measure each segment before optimizing. A slow React page is not necessarily a React problem; the API or database may dominate total latency.',followups:['How do you reduce bundle size?','How do you find a slow SQL query?'],traps:['Do not optimize based only on intuition.']},
{id:'fullstack-038',level:'L3',title:'N+1 queries visible through a React screen',subtopic:'Performance',what:'A UI that renders a list can accidentally expose an N+1 backend query problem when the API loads related data inefficiently.',how:'React may make one request, but Spring/JPA can execute one query for the list and additional queries per item. Detect this with SQL logs or tracing and fix fetching/projection/query design on the backend.',code:`GET /orders -> 1 query\nfor each order -> customer query\nTotal -> 1 + N`,delivery:'I trace the API server-side before assuming the browser is making too many requests. If JPA creates N+1 queries, I fix the repository/query or fetch strategy.',followups:['join fetch?','EntityGraph?','DTO projection?'],traps:['Do not solve backend N+1 by adding arbitrary frontend delays.']},
{id:'fullstack-039',level:'L3',title:'Deployment architecture for React + Spring Boot',subtopic:'Deployment',what:'A production deployment can serve the React static assets separately from the Spring Boot API or place them behind a common edge layer.',how:'A CDN or web server serves the frontend bundle. An API gateway/load balancer routes /api traffic to Spring Boot instances. HTTPS, environment configuration, health checks and observability complete the deployment.',code:`Browser\n ├─ CDN -> React static assets\n └─ HTTPS -> Load Balancer -> Spring Boot`,delivery:'I separate static asset delivery from API compute when it improves caching and scaling, while keeping a clean public HTTPS boundary.',followups:['Where does TLS terminate?','How do you deploy multiple API instances?'],traps:['Do not expose internal service ports directly to the public internet without a reason.']},
{id:'fullstack-040',level:'L3',title:'Blue-green deployment with frontend and backend',subtopic:'Deployment',what:'Blue-green deployment keeps two production environments so traffic can switch between versions with a fast rollback path.',how:'Deploy the new frontend/backend pair to the inactive environment, run smoke tests, switch traffic, monitor, and roll back if required. API compatibility matters during the transition.',code:`BLUE: v1 frontend + compatible API\nGREEN: v2 frontend + compatible API\n        -> switch traffic ->`,delivery:'I make deployment safe by ensuring the new frontend and backend can coexist long enough for traffic to transition cleanly.',followups:['What about database migrations?','How do you roll back schema changes?'],traps:['Do not assume application rollback can reverse an incompatible database migration.']},
{id:'fullstack-041',level:'L3',title:'Database migrations without breaking the frontend',subtopic:'Deployment',what:'Schema changes must be compatible with versions of the backend that may temporarily coexist during deployment.',how:'Use expand-and-contract migrations: add new structures, deploy code that can handle both, migrate data, then remove old structures later.',code:`1. Add new_column\n2. Backend writes both\n3. Frontend moves to new API field\n4. Backfill\n5. Remove old_column later`,delivery:'I avoid destructive schema changes in the same step as an application rollout. Expand-and-contract gives old and new application versions a compatibility window.',followups:['How do you handle renamed fields?','Can frontend and backend deploy independently?'],traps:['Do not drop a column before every running application version stops using it.']},
{id:'fullstack-042',level:'L3',title:'Feature flags across React and Spring Boot',subtopic:'Delivery',what:'Feature flags allow functionality to be enabled or disabled without redeploying the entire product.',how:'The backend can enforce server-side rollout rules while React uses the same flag state to control presentation. Critical authorization must not depend solely on a frontend flag.',code:`if (flags.newCheckout) {\n  return <NewCheckout />;\n}\nreturn <Checkout />;`,delivery:'I use feature flags to decouple deployment from release. The backend remains authoritative for capabilities and security; React uses flags mainly to control experience.',followups:['How do you clean up flags?','How do you roll out to 10% of users?'],traps:['Do not leave temporary flags permanently.']},
{id:'fullstack-043',level:'L3',title:'Audit logging for full-stack actions',subtopic:'Security',what:'Audit logs record important security or business actions with enough context to reconstruct who did what and when.',how:'Spring Boot records authenticated principal, action, target, timestamp, outcome and correlation ID. React should not be trusted as the audit source.',code:`actor=42 action=ORDER_APPROVED target=981\noutcome=SUCCESS traceId=abc123`,delivery:'I generate audit records server-side because the backend can verify identity and the actual state transition.',followups:['Audit log vs application log?','How do you protect audit records?'],traps:['Do not accept actor identity from a browser field as authoritative.']},
{id:'fullstack-044',level:'L3',title:'Handling stale React data after a mutation',subtopic:'Data Fetching',what:'After a mutation, cached or displayed data can become stale if the frontend does not reconcile it with the server result.',how:'Use the mutation response to update relevant state or invalidate/refetch the affected query. Avoid maintaining multiple independent copies of the same server entity.',code:`await updateOrder(id, patch);\nawait invalidate(['orders', id]);`,delivery:'After mutations I deliberately reconcile server state. For complex applications I prefer a consistent server-state strategy rather than ad hoc refetch calls scattered through components.',followups:['Invalidate vs direct cache update?','What is stale-while-revalidate?'],traps:['Do not assume the mutation request succeeded without inspecting its response.']},
{id:'fullstack-045',level:'L3',title:'Designing resilient external API integrations',subtopic:'Reliability',what:'Spring Boot services often depend on external providers that can be slow, unavailable, or inconsistent.',how:'Use timeouts, bounded retries, circuit breakers where appropriate, fallback behavior, idempotency and clear status modeling. React should show a meaningful state rather than pretending the dependency is always available.',code:`React -> Spring Boot -> Payment Provider\n                   timeout/retry/circuit breaker`,delivery:'I isolate external dependency failures from the user experience. The backend owns resilience policies and returns an explicit application state to React.',followups:['Circuit breaker states?','What should the UI show during degraded mode?'],traps:['Do not retry indefinitely or hide dependency failure as success.']},
{id:'fullstack-046',level:'L3',title:'Full-stack security checklist',subtopic:'Security',what:'Security must be enforced across browser, API, identity, data access, transport and operational layers.',how:'Use HTTPS, strong authentication, server-side authorization, input validation, output encoding, safe credential handling, CORS/CSRF controls as applicable, rate limiting, secure headers, dependency updates and auditability.',code:`Browser -> HTTPS -> Gateway -> Spring Security\n                         -> validation\n                         -> authorization\n                         -> data access`,delivery:'I treat the browser as untrusted and enforce security at the backend boundary, while also reducing frontend exposure through safe browser practices.',followups:['XSS?','CSRF?','CORS?','Secrets management?'],traps:['No single security mechanism protects the whole stack.']},
{id:'fullstack-047',level:'L3',title:'Observability for a React + Spring Boot feature',subtopic:'Observability',what:'Observability combines logs, metrics and traces so a team can understand behavior and failures in production.',how:'React can capture client errors and performance signals without logging sensitive data. Spring Boot exposes structured logs, metrics and traces. A shared trace context links the layers.',code:`User action\n -> browser error/performance\n -> trace\n -> Spring metrics/logs\n -> downstream trace`,delivery:'I instrument the feature around user-visible outcomes and server boundaries, then use correlation to move from frontend symptoms to backend causes.',followups:['What metrics would you alert on?','How do you avoid PII in telemetry?'],traps:['Do not log passwords, tokens, or sensitive payloads for debugging.']},
{id:'fullstack-048',level:'L3',title:'Designing an order creation flow',subtopic:'Case Study',what:'Order creation is a useful full-stack case because it combines validation, authorization, transactions, inventory, payment and asynchronous processing.',how:'React collects validated input and submits a command. Spring authorizes and validates, creates the order transactionally, then coordinates inventory/payment or publishes an event depending on the business workflow.',code:`React -> POST /api/orders\n -> authorize\n -> validate\n -> transaction\n -> order + outbox\n -> async fulfillment`,delivery:'I separate the synchronous acceptance of the order from longer-running fulfillment. The exact boundary depends on business consistency requirements.',followups:['Where does payment happen?','How do you prevent duplicate orders?','What if inventory fails?'],traps:['Do not make every downstream operation part of one long database transaction.']},
{id:'fullstack-049',level:'L3',title:'Designing a role-based admin screen',subtopic:'Case Study',what:'An admin screen needs both frontend affordances and backend enforcement.',how:'React uses authenticated user information to show appropriate navigation. Spring Security checks roles/authorities for every protected API operation. The API returns 403 when access is denied.',code:`React: show Admin menu if role is known\nBackend: @PreAuthorize("hasRole('ADMIN')")`,delivery:'The frontend improves usability by hiding irrelevant controls, but every sensitive API operation is protected server-side.',followups:['What if a user edits the request manually?','Where do roles come from?'],traps:['Never trust a role value sent by the browser.']},
{id:'fullstack-050',level:'L3',title:'Full-stack production troubleshooting playbook',subtopic:'Debugging',what:'A repeatable troubleshooting flow prevents teams from making random changes across frontend and backend layers.',how:'Reproduce, capture request details, inspect browser console/network, classify the HTTP result, trace the request through Spring logs and dependencies, inspect database behavior, identify the first failing boundary, then fix and add a regression test.',code:`Reproduce\n -> Browser console/network\n -> HTTP status\n -> Trace ID\n -> Spring logs\n -> Dependency/DB\n -> First failure\n -> Fix + regression test`,delivery:'I troubleshoot from the outside in and use evidence at each boundary. The goal is to identify the first incorrect behavior, not merely the final error message.',followups:['How would you debug a 401?','How would you debug slow responses?','How would you debug stale UI data?'],traps:['Do not change multiple layers at once before isolating the failing boundary.']}
];

import extraFullStackLessons from './fullstack-extra.json';

export const fullStackLessons = [...baseFullStackLessons, ...extraFullStackLessons];
