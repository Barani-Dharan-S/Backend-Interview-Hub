import React, { useMemo, useState } from "react";
import { deepDiveLessons } from "../data/deepDiveLessons";
import { deepDiveLabs } from "../data/deepDiveLabs";

function Card({item,open,onToggle,lab}){const followups=item.followups||[];return <article className="lesson-card scenario-card"><button className="lesson-head" onClick={onToggle}><span className="q-number">{item.id.split('-').pop()}</span><span className="lesson-title"><strong>{item.title}</strong><span className="meta"><i className="level">{item.level}</i><i>{lab?"Integration Lab":item.topic}</i></span></span><span className="chevron">{open?"⌃":"⌄"}</span></button>{open&&<div className="lesson-body scenario-body"><div className="explain-section"><div className="section-kicker">WHAT IS IT?</div><p>{lab?item.situation:item.what}</p></div><div className="explain-section"><div className="section-kicker">WHY / HOW IT WORKS</div><p>{lab?item.goal:item.how}</p></div><div className="explain-section"><div className="section-kicker">{lab?"LAB STEPS / COMMANDS":"IMPLEMENTATION / EXAMPLE"}</div>{lab?<><ol className="mistake-list" style={{marginBottom:10}}>{item.steps.map((s,i)=><li key={i}>{s}</li>)}</ol><pre>{item.commands.join("\n")}</pre></>:<pre>{item.code}</pre>}</div>{!lab&&<div className="explain-section"><div className="section-kicker">REAL-WORLD PERSPECTIVE</div><p>{item.realWorld}</p></div>}<div className="explain-section interview-script"><span className="script-label">INTERVIEW DELIVERY</span><p>{item.delivery}</p></div><div className="explain-section"><div className="section-kicker">EXPECTED FOLLOW-UPS</div><div className="followup-list">{followups.map((q,i)=><details className="followup-item" key={i}><summary>{q}</summary><div className="followup-answer"><strong>ANSWER</strong><p>{item.followupAnswers?.[i]}</p></div></details>)}</div></div><div className="explain-section mistake-section"><div className="section-kicker">COMMON TRAPS</div><ul className="mistake-list">{item.traps.map((t,i)=><li key={i}>{t}</li>)}</ul></div></div>}</article>}

export default function DeepDiveLearning(){const [tab,setTab]=useState("lessons"),[level,setLevel]=useState("all"),[query,setQuery]=useState(""),[open,setOpen]=useState({});const source=tab==="lessons"?deepDiveLessons:deepDiveLabs;const filtered=useMemo(()=>source.filter(x=>(level==="all"||x.level===level)&&JSON.stringify(x).toLowerCase().includes(query.toLowerCase())),[source,level,query]);const expandAll=()=>setOpen(Object.fromEntries(filtered.map(x=>[x.id,true])));return <section className="learning-page"><div className="learning-hero scenario-hero"><span className="pill">V21 · DEEP LEARNING & ARCHITECTURE</span><h2>Understand deeper.<br/><em>Connect the system.</em></h2><p>Go beneath the existing tracks: internals, trade-offs, production behavior, failure modes and cross-stack reasoning. The goal is knowledge you can derive and explain—not evaluation.</p><div className="learning-flow"><span>Understand</span><b>→</b><span>Implement</span><b>→</b><span>Connect</span><b>→</b><span>Break</span><b>→</b><span>Recover</span><b>→</b><span>Explain</span></div></div><div className="learning-toolbar"><div className="search"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search internals, transactions, Kafka, React, Kubernetes, security, observability..."/></div><div className="filters">{["all","L2","L3"].map(x=><button key={x} className={level===x?"filter active":"filter"} onClick={()=>setLevel(x)}>{x==="all"?"All levels":x}</button>)}</div></div><div className="subtopic-row"><button className={tab==="lessons"?"subtopic active":"subtopic"} onClick={()=>{setTab("lessons");setOpen({})}}>🧠 Deep Dive Lessons <small>{deepDiveLessons.length}</small></button><button className={tab==="labs"?"subtopic active":"subtopic"} onClick={()=>{setTab("labs");setOpen({})}}>🛠 Integration Labs <small>{deepDiveLabs.length}</small></button></div><div className="result-row"><span><b>{tab==="lessons"?"Deep Dive Lessons":"Integration Labs"}</b> · {filtered.length} items</span><button onClick={expandAll}>Expand all</button><button onClick={()=>setOpen({})}>Collapse all</button></div><section className="lesson-list">{filtered.map(x=><Card key={x.id} item={x} lab={tab==="labs"} open={!!open[x.id]} onToggle={()=>setOpen(p=>({...p,[x.id]:!p[x.id]}))}/>)}</section></section>}
