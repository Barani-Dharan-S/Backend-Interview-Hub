# V12.18 — Full Stack Development Expansion

- Expanded React + Spring Boot track from 50 to 80 lessons.
- Focus: real application workflows, CRUD, DTOs, validation, authentication/authorization, pagination, search, optimistic updates, file uploads, transactions, concurrency, caching, API evolution, testing, configuration, deployment, CORS, rate limiting, tracing, health checks, CI/CD, compatibility rollouts and production readiness.
- No sidebar/UI redesign. Existing navigation remains the baseline.
- Full Stack count in Header is now dynamic from fullStackLessons.length.
- Full Stack data extension is stored in JSON to avoid JavaScript template-literal interpolation bugs in code examples.
